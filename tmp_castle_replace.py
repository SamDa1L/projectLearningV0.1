# -*- coding: utf-8 -*-
from pathlib import Path
text = Path("Docs/monster-system-0.2-plan.md").read_text(encoding="utf-8")
start = text.index("3. **CastleDB Demo 工程搭建**")
end = text.index("**验收标准**")
new_block = """3. **CastleDB Demo 工程搭建**（参见 `Docs/castledb_basic_manual.md`）  \n   - 在项目根目录创建 `Data/CastleDbDemo/`（`Data` 的子目录），存放 `.cdb` 源文件与导出产物。  \n   - 打开 CastleDB，执行 `File → New`，将工程保存为 `Data/CastleDbDemo/MonsterSystem.cdb`。界面上方为菜单，底部提供 `New Sheet / New Column / New Line / Edit Types`，左上角可见 `Add a column`。  \n   - 建表与建列：  \n     1. 通过底部 `New Sheet` 依次创建 `NPC`、`Ability`、`Player`、`DetectionZone` 等表（如需重命名，可右键底部标签）。  \n     2. 选中某张表后点击 `Add a column`，在弹窗中填列名、类型并设置 `Required`。推荐设置：  \n        - `id`：`Unique Identifier`，Required。  \n        - `displayName`、`animationTrigger`：`Text`，Required。  \n        - 数值（`maxHealth`、`attackDamage`、`moveSpeed`、`attackRange`、`attackCooldown` 等）：`Float` 或 `Integer`，Required。  \n        - 布尔（`enableDeathAnimation` 等）：`Boolean`，可设置默认值。  \n        - 枚举列：先在底部 `Edit Types` 创建 Enum（detectionZone `role` 包含 `PrimaryAttack/SecondaryAttack/Cliff/Alert/Lookout/Custom`），再在列上选择该 Enum 并勾选 Required。  \n        - `detectionZones`：列类型选 `List`，CastleDB 会提示选择列表元素类型，点击 `Custom Type`（如命名 `DetectionZoneEntry`），在其中添加 `role`（Enum，Required）与 `childId`（Text，Required，表示 Prefab 子物体名称）。未来若需描述碰撞盒，可在该 `Custom Type` 中继续添加 `colliderType/size/offset`。  \n        - 资源路径 / Prefab 名称使用 `Text` 或 `File`，同样勾选 Required。  \n   - 底部 `New Line` 可新增数据行；为每张表录入至少 1~2 条示例数据（建议 Knight、FlyingEye）。  \n   - 完成编辑后使用 `Dev → Export JSON`（若版本不同，可在 `Dev` 菜单查找 `Export`/`Dump JSON`）导出为 `Assets/StreamingAssets/CastleDbDemo/CastleDbDemo.json`。  \n   - 在 Unity 中借助 `Assets/Scripts/Data/CastleDB/CastleDbJsonDump.cs` 等脚本验证 JSON，并在 `Logs/NotesLog/CodexProjectLogs/0.2基线快照.md` 记录 `.cdb` 与 JSON 路径及导出步骤，方便团队复现。\n\n"""
text = text[:start] + new_block + text[end:]
Path("Docs/monster-system-0.2-plan.md").write_text(text, encoding="utf-8")
