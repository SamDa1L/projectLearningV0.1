# CLAUDE.md — projectLearningV0.1

> Claude Code 项目上下文配置文件。提供快速代码定位、架构理解和开发进度追踪。

---

## 项目状态总览

- **当前版本**：0.5（能力系统扩展 + 状态/遗物系统）✅ 已完成
- **Unity 版本**：2022.3.14f1c1 (URP 2D)
- **数据架构**：多文件 CastleDB（Monster/Player/PlayerAbility/Item/Status/Relic）+ Provider 模式
- **核心系统**：怪物系统 + 玩家能力系统 + 物品拾取系统 + HUD + 替换面板 + 状态系统 + 遗物系统 + 能力扩展（Dash/Buff/Summon/AttackOverride）
- **最后更新**：2026-01-26

---

## 快速信息

| 项目 | 值 |
|------|-----|
| Unity 版本 | 2022.3.14f1c1 (URP 2D) |
| **当前分支** | `Project_Test` |
| **主开发测试场景** | `Assets/Scenes/NPCTestScenes/TestEnemy.unity` |
| 备用场景 | `Assets/Scenes/GamePlayScene.unity`（不再作为主要开发场景） |
| CastleDB 数据（当前） | `Assets/Resources/Data/` 目录下六个文件 |
| 数据文件 | `MonsterSystem.cdb` / `Player.cdb` / `PlayerAbility.cdb` / `Item.cdb` / `Status.cdb` / `Relic.cdb` |
| 权威规划文档（0.2） | `Docs/monster-system-0.2-plan.md` |
| 权威规划文档（0.3） | `Logs/NotesLog/CodexProjectLogs/0.3版本规划计划.md` |
| 权威规划文档（0.4） | `Logs/NotesLog/CodexProjectLogs/0.4版本规划计划.md` |
| 权威规划文档（0.5） | `Logs/NotesLog/CodexProjectLogs/0.5版本计划规划.md` |
| 权威规划文档（0.55） | `Logs/NotesLog/CodexProjectLogs/0.55版本计划规划中.md` |

### 最近提交记录
```
87ad200 0.5版本优化2.3内容
d81a679 0.5版本优化第一步完成
e907ceb 0.5版本优化
b555cda 0.5版本召唤系统修改
3ae1d2a 0.5版本开始铺量
0559142 0.5版本完结
```

### 关键 Packages
- `com.unity.inputsystem@1.7.0` - 新输入系统
- `com.unity.render-pipelines.universal@14.0.9` - URP
- `com.unity.cinemachine@2.9.7` - 摄像机
- `com.unity.test-framework@1.1.33` - 测试框架

---

## 开发工作标准

**场景规范（2025-12-27）**：
- ✅ **主开发测试场景**：`Assets/Scenes/NPCTestScenes/TestEnemy.unity`
- ⚠️ **已弃用**：`Assets/Scenes/GamePlayScene.unity` 不再作为主要开发测试场景
- **所有新功能**：测试、验证、调试都在 TestEnemy 场景进行
- **场景配置**：GameObject 设置、组件配置都以 TestEnemy 为准
- **PlayMode 验证**：所有运行时验证以 TestEnemy 场景为基准

---

## 版本开发进度

### 版本 0.5（能力系统扩展 + 状态/遗物系统）—— ✅ 已完成

**状态**：已完成 | **文档版本**：1.0 | **更新日期**：2026-01-26

**核心目标**：
- ✅ 扩展能力类型（Dash/Buff/Summon/AttackOverride）
- ✅ 实现状态系统（Status/Modifier）
- ✅ 实现遗物系统（Relic/被动效果）
- ✅ 能力事件系统（onCast/onHit/onKill）
- ✅ 输入系统优化（手柄支持 + 设备切换）
- ✅ HUD 键位图标显示

| 阶段 | 状态 | 描述 |
|------|------|------|
| Phase 0 | ✅ 已完成 | 基线验证（0.4 功能完整性） |
| Phase 1 | ✅ 已完成 | Status 数据源 + StatusCatalog + Provider |
| Phase 2 | ✅ 已完成 | Modifier 系统（StatModifierLayer + 叠加规则） |
| Phase 3 | ✅ 已完成 | Relic 数据源 + RelicCatalog + Provider |
| Phase 4 | ✅ 已完成 | 能力事件系统（Event → Effect 执行器） |
| Phase 5 | ✅ 已完成 | 能力扩展（Dash/Buff/Summon/AttackOverride） |
| Phase 6 | ✅ 已完成 | 能力类型样例落地（每种至少一个可玩样例） |
| Phase 7 | ✅ 已完成 | 遗物被动能力实现 |
| Phase 8 | ✅ 已完成 | HUD 键位图标显示 |
| Phase 9 | ✅ 已完成 | 输入系统优化（手柄支持 + 设备切换） |

**总工时**：约 120h | **权威规划文档**：`Logs/NotesLog/CodexProjectLogs/0.5版本计划规划.md`

#### 0.5 核心完成要点

##### Phase 1-2: Status & Modifier 系统

1. **Status.cdb 数据源**
   - 位置：`Assets/Resources/Data/Status.cdb`
   - 字段：id/displayName/defaultDuration/stackRule/maxStacks/modifiersJson
   - 示例：`Freeze`（冰冻状态，moveSpeedMultiplier: 0.1）

2. **StatusCatalog ScriptableObject**
   - 位置：`Assets/Resources/Config/StatusCatalog.asset`
   - 功能：索引化查询（TryGetStatus/GetAllStatuses）

3. **StatModifierLayer 系统**
   - 位置：`Assets/Scripts/Player/StatModifierLayer.cs`
   - 功能：属性修改器叠加（Add/Multiply/Override）
   - 支持：moveSpeed/attackDamage/defense 等属性

##### Phase 3: Relic 系统

1. **Relic.cdb 数据源**
   - 位置：`Assets/Resources/Data/Relic.cdb`
   - 字段：id/kind/paramsJson
   - 示例：`relic_shield_test`（护盾遗物）

2. **RelicCatalog ScriptableObject**
   - 位置：`Assets/Resources/Config/RelicCatalog.asset`
   - 功能：索引化查询（TryGetRelic/GetAllRelics）

##### Phase 4-5: 能力事件与扩展

1. **能力事件系统**
   - 事件类型：onCast/onHit/onKill/onDamaged
   - Effect 执行器：ApplyStatus/SpawnVfx/PlaySound/ModifyStat

2. **扩展能力类型**
   - **Dash**：冲刺能力（distance/speed/invincibleWindow）
   - **Buff**：主动增益（duration/modifiersJson/stackRule）
   - **Summon**：召唤物（prefabPath/lifetime/maxCount/spawnRule）
   - **AttackOverride**：攻击方式替换（projectileId/damageMultiplier）

##### Phase 8-9: HUD & 输入优化

1. **HUD 键位图标**
   - 位置：`Assets/Scripts/UI/HudPresenter.cs`
   - 功能：根据输入设备显示对应键位图标（键盘/手柄）
   - 配置：`InputIconCatalog.asset`

2. **输入设备切换**
   - 位置：`Assets/Scripts/Input/InputDeviceSwitcher.cs`
   - 功能：自动检测最后使用设备并切换 ActionMap
   - 支持：键盘/鼠标/手柄

**关键改进（0.5）**:
- ✅ 能力类型扩展：从单一 Attack 扩展到 Dash/Buff/Summon/AttackOverride
- ✅ 状态系统：支持 Buff/Debuff 的叠加、刷新、替换规则
- ✅ 遗物系统：支持被动效果（护盾/属性加成）
- ✅ 事件驱动：能力触发事件 → Effect 执行器模式
- ✅ 输入优化：手柄支持 + 设备自动切换 + HUD 图标联动

**DoD（0.5 完成标准）**：
- ✅ Status.cdb/Relic.cdb 导入无错误
- ✅ Dash/Buff/Summon/AttackOverride 各有至少一个可玩样例
- ✅ 状态叠加规则（Refresh/Add/Ignore/Replace）正确工作
- ✅ 遗物被动效果生效（护盾/属性修改）
- ✅ HUD 键位图标根据输入设备切换
- ✅ PlayMode 测试覆盖核心流程

### 版本 0.55（能力系统进阶质量提升）—— 🔄 规划中

**状态**：规划中 | **文档版本**：0.1 | **更新日期**：2026-01-26

**核心目标**：
- 🔄 Dash 碰撞稳定性（不穿墙/不卡墙/状态互斥清晰）
- 🔄 Buff uniqueKey 跨技能共享（叠加/刷新/互斥策略）
- 🔄 Summon 管理器（统一计数/回收/清理）
- 🔄 AttackOverride 数据表达升级（ProjectileReplace/HitboxReplace）
- 🔄 Provider 校验强化（参数范围/合法值/白名单）
- 🔄 回归测试补齐（集成测试 + 与 0.4 链路共存验证）

**权威规划文档**：`Logs/NotesLog/CodexProjectLogs/0.55版本计划规划中.md`

**差距项清单**（从 0.5 样例 → 0.55 稳定质量）：
- Dash：缺少碰撞处理/距离裁剪，状态机边界不够清晰
- Buff：uniqueKey 未生效，modifiersJson 字段语义未标准化
- Summon：缺少管理器，owner/team 规则不明确
- AttackOverride：数据语义与规划不完全一致，与 PlayerAttackOverride 边界需固化
- 横向：Provider 校验偏存在性，样例场景覆盖不全，回归维度不足

**建议交付节奏**：
1. Dash 碰撞裁剪 + Provider 校验 + PlayMode
2. Buff uniqueKey + 控制器抽离 + 事件 + PlayMode
3. Summon 控制器 + 清理策略 + spawnRule 校验 + PlayMode
4. AttackOverride schema 定义 + Controller + 回归测试
5. 场景验收区补齐（TestEnemy.unity 或新建测试场景）

### 版本 0.4（Item/拾取/HUD 系统）—— ✅ 已完成

| 阶段 | 状态 | 描述 |
|------|------|------|
| Stage 1 | ✅ 已完成 | CastleDB 接口层（DTO/Service/日志/测试） |
| Stage 2A | ✅ 已完成 | 敌人数值链路（CastleDB → Profile → Runtime） |
| Stage 2B | ✅ 已完成 | 检测区/zoneBindings 一致性 |
| Stage 3A | ✅ 已完成 | 玩家基础属性 & 攻击伤害链路 |
| Stage 3B | ✅ 已完成 | 能力系统接口（AbilityCatalog/IPlayerAbility/调度器）+ 严格校验 |
| Stage 4 | ✅ 已完成 | Prefab 同步 & 热更新 & 工作流文档化 |

### 版本 0.2（Monster System）—— ✅ 已完成

| 阶段 | 状态 | 描述 |
|------|------|------|
| Stage 1 | ✅ 已完成 | CastleDB 接口层（DTO/Service/日志/测试） |
| Stage 2A | ✅ 已完成 | 敌人数值链路（CastleDB → Profile → Runtime） |
| Stage 2B | ✅ 已完成 | 检测区/zoneBindings 一致性 |
| Stage 3A | ✅ 已完成 | 玩家基础属性 & 攻击伤害链路 |
| Stage 3B | ✅ 已完成 | 能力系统接口（AbilityCatalog/IPlayerAbility/调度器）+ 严格校验 |
| Stage 4 | ✅ 已完成 | Prefab 同步 & 热更新 & 工作流文档化 |

### 版本 0.3（CastleDB 多数据源重构）—— ✅ 已完成

**状态**：已完成 | **文档版本**：3.0 | **更新日期**：2025-12-24

**重构目标**：
- ✅ 将单一 `MonsterSystem.cdb` 拆分为多个独立文件（Monster/Player/PlayerAbility）
- ✅ 建立可扩展的多数据源架构（Provider + Registry 模式）
- ✅ 支持单文件右键导入 + 跨文件引用解析
- ✅ 数据迁移完成，所有编辑器工具和测试已更新

| 阶段 | 状态 | 描述 | 实际工时 |
|------|------|------|----------|
| Phase 1 | ✅ 已完成 | 基础设施（ICdbDataProvider/Registry/ModuleDescriptor） | 10h |
| Phase 2 | ✅ 已完成 | MonsterDataProvider 实现 | 11.5h |
| Phase 3 | ✅ 已完成 | 表结构设计（Player.cdb/PlayerAbility.cdb） | 2h |
| Phase 4 | ✅ 已完成 | PlayerDataProvider + AbilityDataProvider + Import All 重构 | 15h |
| Phase 5 | ✅ 已完成 | 右键导入优化 + 原子导入 + 失败回滚 | 12h |
| Phase 6 | ✅ 已完成 | 数据迁移修复（displayName/Legacy清理/路径更新）| 4h |
| Phase 7 | ✅ 已完成 | Unity 编辑器验证（Import All/PlayMode 测试/产物验证）| 8h |

**总工时**：62.5h | **权威规划文档**：`Logs/NotesLog/CodexProjectLogs/0.3版本规划计划.md`

### 版本 0.5（能力系统扩展 + 状态/遗物系统）—— ✅ 已完成

**状态**：已完成 | **文档版本**：1.0 | **更新日期**：2026-01-26

**核心目标**：
- ✅ 扩展能力类型（Dash/Buff/Summon/AttackOverride）
- ✅ 实现状态系统（Status/Modifier）
- ✅ 实现遗物系统（Relic/被动效果）
- ✅ 能力事件系统（onCast/onHit/onKill）
- ✅ 输入系统优化（手柄支持 + 设备切换）
- ✅ HUD 键位图标显示

| 阶段 | 状态 | 描述 |
|------|------|------|
| Phase 0 | ✅ 已完成 | 基线验证（0.4 功能完整性） |
| Phase 1 | ✅ 已完成 | Status 数据源 + StatusCatalog + Provider |
| Phase 2 | ✅ 已完成 | Modifier 系统（StatModifierLayer + 叠加规则） |
| Phase 3 | ✅ 已完成 | Relic 数据源 + RelicCatalog + Provider |
| Phase 4 | ✅ 已完成 | 能力事件系统（Event → Effect 执行器） |
| Phase 5 | ✅ 已完成 | 能力扩展（Dash/Buff/Summon/AttackOverride） |
| Phase 6 | ✅ 已完成 | 能力类型样例落地（每种至少一个可玩样例） |
| Phase 7 | ✅ 已完成 | 遗物被动能力实现 |
| Phase 8 | ✅ 已完成 | HUD 键位图标显示 |
| Phase 9 | ✅ 已完成 | 输入系统优化（手柄支持 + 设备切换） |

**总工时**：约 120h | **权威规划文档**：`Logs/NotesLog/CodexProjectLogs/0.5版本计划规划.md`

#### 0.5 核心完成要点

##### Phase 1-2: Status & Modifier 系统

1. **Status.cdb 数据源**
   - 位置：`Assets/Resources/Data/Status.cdb`
   - 字段：id/displayName/defaultDuration/stackRule/maxStacks/modifiersJson
   - 示例：`Freeze`（冰冻状态，moveSpeedMultiplier: 0.1）

2. **StatusCatalog ScriptableObject**
   - 位置：`Assets/Resources/Config/StatusCatalog.asset`
   - 功能：索引化查询（TryGetStatus/GetAllStatuses）

3. **StatModifierLayer 系统**
   - 位置：`Assets/Scripts/Player/StatModifierLayer.cs`
   - 功能：属性修改器叠加（Add/Multiply/Override）
   - 支持：moveSpeed/attackDamage/defense 等属性

##### Phase 3: Relic 系统

1. **Relic.cdb 数据源**
   - 位置：`Assets/Resources/Data/Relic.cdb`
   - 字段：id/kind/paramsJson
   - 示例：`relic_shield_test`（护盾遗物）

2. **RelicCatalog ScriptableObject**
   - 位置：`Assets/Resources/Config/RelicCatalog.asset`
   - 功能：索引化查询（TryGetRelic/GetAllRelics）

##### Phase 4-5: 能力事件与扩展

1. **能力事件系统**
   - 事件类型：onCast/onHit/onKill/onDamaged
   - Effect 执行器：ApplyStatus/SpawnVfx/PlaySound/ModifyStat

2. **扩展能力类型**
   - **Dash**：冲刺能力（distance/speed/invincibleWindow）
   - **Buff**：主动增益（duration/modifiersJson/stackRule）
   - **Summon**：召唤物（prefabPath/lifetime/maxCount/spawnRule）
   - **AttackOverride**：攻击方式替换（projectileId/damageMultiplier）

##### Phase 8-9: HUD & 输入优化

1. **HUD 键位图标**
   - 位置：`Assets/Scripts/UI/HudPresenter.cs`
   - 功能：根据输入设备显示对应键位图标（键盘/手柄）
   - 配置：`InputIconCatalog.asset`

2. **输入设备切换**
   - 位置：`Assets/Scripts/Input/InputDeviceSwitcher.cs`
   - 功能：自动检测最后使用设备并切换 ActionMap
   - 支持：键盘/鼠标/手柄

**关键改进（0.5）**:
- ✅ 能力类型扩展：从单一 Attack 扩展到 Dash/Buff/Summon/AttackOverride
- ✅ 状态系统：支持 Buff/Debuff 的叠加、刷新、替换规则
- ✅ 遗物系统：支持被动效果（护盾/属性加成）
- ✅ 事件驱动：能力触发事件 → Effect 执行器模式
- ✅ 输入优化：手柄支持 + 设备自动切换 + HUD 图标联动

**DoD（0.5 完成标准）**：
- ✅ Status.cdb/Relic.cdb 导入无错误
- ✅ Dash/Buff/Summon/AttackOverride 各有至少一个可玩样例
- ✅ 状态叠加规则（Refresh/Add/Ignore/Replace）正确工作
- ✅ 遗物被动效果生效（护盾/属性修改）
- ✅ HUD 键位图标根据输入设备切换
- ✅ PlayMode 测试覆盖核心流程

### 版本 0.4（Item/拾取/HUD 系统）—— ✅ 已完成

**状态**：已完成 | **文档版本**：2.0 | **更新日期**：2025-12-29

**核心目标**：
- ✅ 补齐 Item/拾取/替换/HUD/UI 快配链路
- ✅ 在既有多 Provider 架构上保持导入原子性与测试质量
- ✅ 实现 Ability 槽位管理与替换流程
- ✅ 实现 HUD 实时更新（能力槽/血瓶/血条）
- ✅ 实现 Excel 导出（CdbImportRoot 配置 + 路径镜像）

| 阶段 | 状态 | 描述 | 实际工时 |
|------|------|------|----------|
| Phase 0 | ✅ 已完成 | schemaVersion 迁移到 0.4 + 单一真相源 | 2h |
| Phase 1 | ✅ 已完成 | Item.cdb/DTO/ItemDataProvider/导入产物/跨文件引用校验 | 10h |
| Phase 2 | ✅ 已完成 | CastleDbService Item 查询接口 + IsValid + 程序集边界修复 | 2.5h |
| Phase 3 | ✅ 已完成 | PlayerInventory（数据/TryPickup/事件） | 8h |
| Phase 4 | ✅ 已完成 | ItemPickup（交互/RequireReplace 锁定） | 6h |
| Phase 5 | ✅ 已完成 | AbilitySystem Enable/Disable 队列化 | 4h |
| Phase 6 | ✅ 已完成 | PlayerEquipmentController（能力启停映射） + GameBootstrap 集成 | 2h |
| Phase 7 | ✅ 已完成 | HUD（HudBinding/HudPresenter/Health 事件） | 4h |
| Phase 8 | ✅ 已完成 | AbilityReplacePanel + ReplaceController | 3h |
| Phase 9 | ✅ 已完成 | Authoring Pipeline（icon 规范/PickupItem prefab/ItemPickup Inspector） | 2h |
| Phase 10 | ✅ 已完成 | HUD Quick Config 工具（Create Template/AutoBind/Validate） | 2h |
| Phase 11 | ✅ 已完成 | EditMode/PlayMode 测试 | 2h |
| Phase 12 | ✅ 已完成 | CDB Excel 导出（契约 [C-Tool-2]） | 8h |

**已完成工时**：58.5h | **权威规划文档**：`Logs/NotesLog/CodexProjectLogs/0.4版本规划计划.md`

#### Phase 0 & Phase 1 完成要点（2025-12-25）

##### Phase 0: schemaVersion 迁移（已完成）

1. **统一版本号更新**
   - 位置：所有 .cdb 文件的 Meta Sheet
   - 更新：`MonsterSystem.cdb`, `Player.cdb`, `PlayerAbility.cdb` 的 `schemaVersion: "0.3"` → `"0.4"`
   - 验证：Import All 步骤 3/7 通过版本一致性校验

2. **单一真相源模式**
   - 位置：`CdbDataProviderRegistry.cs:42`
   - 实现：`ExpectedSchemaVersion = "0.4"` 作为唯一版本定义
   - 清理：移除 `CdbImportCoordinator` 和 `CastleDbImporter` 中的硬编码版本常量
   - 效果：所有版本检查统一引用 `CdbDataProviderRegistry.ExpectedSchemaVersion`

##### Phase 1: Item 数据链路（已完成）

1. **ItemDefinition DTO 体系**
   - 位置：`Runtime/ItemDefinition.cs`
   - 类型：`ItemType` 枚举（Ability/Consumable/Material）
   - 结构：`ItemConsumeEffect` 结构体（heal 字段）
   - DTO：`ItemDefinition` 类（id/displayName/itemType/icon/abilityId/maxStack/consumeEffect/consumeEffectRawJson/uiTag）
   - Entry：`ItemEntry` 类（CastleDB 原始数据映射）

2. **ItemCatalog ScriptableObject**
   - 位置：`Scripts/Data/CastleDB/Runtime/ItemCatalog.cs`
   - 功能：索引化查询（OnEnable 构建 Dictionary<string, ItemDefinition>）
   - 接口：`TryGetItem(itemId, out ItemDefinition)`（不存在返回 false，不打印日志）
   - 校验：`GetAllItems()` 返回副本防止外部修改
   - 腐化检测：ID 重复时 Clear Dictionary 并打印 Error

3. **ItemDataProvider 完整实现**
   - 位置：`Editor/Providers/ItemDataProvider.cs`
   - 职责：解析 Item Sheet、校验数据、生成 ItemCatalog.asset
   - 核心校验：
     - ID 非空唯一性
     - itemType 枚举合法性（大小写敏感）
     - abilityId 必填性（itemType=Ability 时）
     - **跨文件引用校验**：abilityId 必须存在于 PlayerAbility 数据中（依赖 AbilityDataProvider）
     - consumeEffectJson 格式校验（必须是 JSON 对象，支持 heal:number）
     - maxStack > 0 校验
   - 导入产物：`Assets/Resources/Config/ItemCatalog.asset`

4. **Item.cdb 数据文件**
   - 位置：`Assets/Resources/Data/Item.cdb`
   - 示例数据：
     - `potion_red`（Consumable 类型，consumeEffectJson: `{"heal":50}`）
     - `ability_dash`（Ability 类型，abilityId: `"BasicRun"`）
   - Meta Sheet：
     - `schemaVersion: "0.4"`
     - `providerId: "Item"`
     - `dependencies: "PlayerAbility"`（声明依赖关系）
     - `resourcePath: "Data/Item"`

5. **Provider 注册与集成**
   - 位置：`Editor/CdbProviderBootstrap.cs:65-70`
   - 注册：`ItemDataProvider` 在启动时自动注册到 Registry
   - 效果：Import All 自动识别并导入 Item.cdb

##### Phase 2: CastleDbService Item 查询接口（已完成）

1. **ICastleDbService 接口扩展**
   - 位置：`Runtime/ICastleDbInterfaces.cs:143-164`
   - 新增方法：
     - `bool TryGetItem(string itemId, out ItemDefinition def)`
     - `IReadOnlyList<ItemDefinition> GetAllItems()`
     - `bool IsValid { get; }`

2. **CastleDbService 实现**
   - 位置：`Runtime/CastleDbService.cs:26-310`
   - 新增字段：
     - `private ItemCatalog _itemCatalog`（懒加载缓存）
     - `private static readonly ItemDefinition[] EmptyItems`（零分配）
   - 实现策略：
     - 懒加载：首次调用 TryGetItem/GetAllItems 时自动 `Resources.Load<ItemCatalog>("Config/ItemCatalog")`
     - 零分配：GetAllItems() 直接返回 items 数组（通过 IReadOnlyList 暴露）
     - 有效性检查：IsValid 属性检查 ItemCatalog 是否加载成功且有效（无 ID 重复等腐化）

3. **程序集边界修复**
   - 问题：ItemCatalog 原位于 Game.Runtime 程序集，CastleDbService 位于 CastleDB.Runtime，造成循环依赖
   - 解决方案：将 ItemCatalog.cs 从 `Assets/Scripts/Data/` 移动到 `Assets/Scripts/Data/CastleDB/Runtime/`
   - 命名空间：添加 `namespace CastleDB.Runtime`
   - GUID 更新：ItemCatalog.asset 的 m_Script GUID 从 `4f0a3a31ac42b0046af566b09b5d0e9e` 更新为 `765868b32c1d2eb4883aa96a61b229a2`

4. **SetItemCatalog 可选注入**
   - 方法：`public void SetItemCatalog(ItemCatalog itemCatalog)`
   - 用途：允许 GameBootstrap 显式注入（可选，懒加载为主要方式）
   - 日志：注入成功后输出 Item 数量

##### Phase 3: PlayerInventory 完整实现（已完成，2025-12-25）

1. **GameplayConfig 扩展**
   - 位置：`Scripts/Gameplay/GameplayConfig.cs:73-76`
   - 新增字段：`int potionMaxCount = 99`（血瓶最大携带数量，0.4 版本 Consumable 上限唯一真相源）
   - 校验：OnValidate 中添加 `potionMaxCount = Mathf.Max(1, potionMaxCount)`

2. **数据结构和枚举**
   - `Scripts/Player/PickupResult.cs`：拾取结果枚举（Success/RequireReplace/Failed_InvalidItemId/Failed_AlreadyEquipped/Failed_NotSupported）
   - `Scripts/Player/PickupRequest.cs`：拾取请求结构体（itemId/amount/sourcePickup）
   - `Scripts/Player/PendingReplaceContext.cs`：待替换上下文结构体（pendingItemId/pendingAmount/sourcePickup）

3. **PlayerInventory 核心实现**
   - 位置：`Scripts/Player/PlayerInventory.cs`
   - 数据：4 槽 `_abilityItemIds` + `_potionCount`
   - 依赖注入：`Initialize(ICastleDbService items, GameplayConfig cfg)` 只允许调用一次
   - 只读访问：`GetAbilityItemId(slotIndex)` / `PotionCount` / `GetPotionMaxCount()`
   - 事件：`OnAbilitySlotChanged(slot, oldItemId, newItemId)` / `OnPotionCountChanged(newCount)`

4. **TryPickup 拾取流程**
   - 签名：`PickupResult TryPickup(PickupRequest req, out PendingReplaceContext ctx)`
   - Ability 拾取逻辑（按契约 [C-Runtime-2] 顺序）：
     1) amount 必须为 1
     2) abilityId 必须非空
     3) 重复检查优先：若任意槽位已有同一 abilityId → Failed_AlreadyEquipped
     4) 若存在空槽：找到首个空槽并写入 → Success
     5) 槽满且 sourcePickup 为空：Error + Failed_NotSupported
     6) 槽满：RequireReplace，ctx 包含 pendingItemId/amount/sourcePickup
   - Consumable 拾取逻辑：
     - 若 potionCount >= PotionMaxCount → Failed_NotSupported
     - 否则累加并 clamp 到上限，触发 OnPotionCountChanged → Success
   - Material：返回 Failed_NotSupported

5. **Equip/Clear 管理**
   - `bool EquipAbilityItemToSlot(int slotIndex, string itemId)`：
     - 校验：slotIndex 0~3、itemId 存在、itemType=Ability、abilityId 非空、重复检查
     - 若写入同值 → false（不触发事件）
     - 否则写入并触发 OnAbilitySlotChanged → true
   - `bool ClearAbilitySlot(int slotIndex)`：
     - 若本来为空 → false
     - 否则清空并触发 OnAbilitySlotChanged → true

6. **一次性日志去重**
   - 使用 `HashSet<string> _loggedErrors` 记录已输出的日志 key
   - Failed_InvalidItemId/Failed_AlreadyEquipped/配置错误等按 itemId/abilityId 去重

7. **ItemPickup 存根**
   - 位置：`Scripts/Gameplay/ItemPickup.cs`
   - Phase 3 职责：提供基本字段（itemId/amount/autoPickup）供结构体引用
   - Phase 4 职责：实现交互逻辑（OnTriggerEnter2D）、锁定机制（SetLocked）、拾取流程

##### Phase 4: ItemPickup 完整实现（已完成，2025-12-25）

1. **PlayerContext 统一服务暴露**
   - 位置：`Scripts/Player/PlayerContext.cs`
   - 必需依赖：`PlayerInventory`、`Damageable`、`AbilitySystem`（缺失则 InteractionEnabled=false）
   - 建议依赖：`ReplaceController`（缺失则 Error 一次性，仅禁用 Replace 功能）
   - 可选依赖：`PlayerEquipmentController`、`PlayerInput`
   - 装配时机：Awake 通过 GetComponent 缓存引用（不查询 CastleDbService）
   - 依赖定位：ItemPickup 通过 `GetComponentInParent<PlayerContext>()` 获取

2. **存根类创建**
   - `Scripts/UI/ReplaceController.cs`：提供 `IsSelecting` 属性和 `BeginReplace()` 方法声明（Phase 8 完整实现）
   - `Scripts/Player/PlayerEquipmentController.cs`：类型声明（Phase 6 完整实现）

3. **ItemPickup 完整实现**
   - 位置：`Scripts/Gameplay/ItemPickup.cs`
   - 状态机：Idle（可拾取）→ Locked（RequireReplace 锁定）→ Destroyed（拾取成功）
   - 组件依赖：`[RequireComponent(typeof(Collider2D))]` 确保必需组件

4. **交互逻辑**
   - `OnTriggerEnter2D`：首次触发拾取
   - `OnTriggerStay2D`：解锁后重试拾取（0.15秒节流）
   - 门控检查：`playerCtx==null` / `!InteractionEnabled` / `IsSelecting` → 直接返回
   - 拾取流程：构造 `PickupRequest(itemId, amount, this)` → 调用 `Inventory.TryPickup()`

5. **结果处理**
   - Success：`Destroy(gameObject)`（可扩展特效/音效）
   - RequireReplace：`SetLocked(true)` → `ReplaceController.BeginReplace(ctx)`（ReplaceController 缺失则解锁）
   - Failed_InvalidItemId：Warning（一次性去重）
   - Failed_AlreadyEquipped：Info（一次性去重）
   - Failed_NotSupported：不输出日志（避免噪音）

6. **锁定机制**
   - API：`void SetLocked(bool locked)` / `bool IsLocked { get; }`
   - 效果：`_collider.enabled = !locked`（禁用 Collider 避免再次触发）
   - 节流清空：进入 Locked 时 `_nextRetryTime = 0f`（解锁后可立即再次尝试）

7. **OnValidate 数据校验**（Editor-only）
   - amount <= 0：自动修正为 1 并 Warning
   - itemType=Ability：强制 amount=1 并 Warning
   - itemType=Material：Error（0.4 不支持拾取）
   - 实例去重：使用 `HashSet<int>` 防止多次执行

8. **一次性日志去重**
   - autoPickup=false：Warning 提示"0.4 不支持手动拾取"（按 itemId 去重）
   - 拾取失败日志：按 `"{result}_{itemId}"` 去重

##### Phase 4 补充修复（2025-12-25）

**问题**：Phase 4 初始实现存在验收闭环缺陷，导致 RequireReplace 流程不可用

**修复内容**：

1. **ReplaceController 降级处理实现**
   - 位置：`Scripts/UI/ReplaceController.cs`
   - 问题：存根版本 `IsSelecting` 恒为 false，`BeginReplace` 仅 Warning，导致拾取物被锁定但无法解锁
   - 修复：
     - 实现 State 枚举（Idle/Selecting）+ `_state` 字段
     - BeginReplace 三重校验：
       1. `sourcePickup==null`：Error（一次性）+ 不进入 Selecting
       2. `_state!=Idle`：Error + 解锁 sourcePickup
       3. `!_initialized`：Warning（一次性）+ 解锁 sourcePickup（Phase 4 降级）
     - Phase 4 降级策略：直接解锁 pickup，避免永久锁定
     - 提供 `Initialize()` 空实现（Phase 8 完整实现时注入依赖）
   - 闭环验证：RequireReplace 流程现在可以正确解锁拾取物

2. **PlayerContext AbilitySystem 校验修复**
   - 位置：`Scripts/Player/PlayerContext.cs`
   - 问题：未校验 AbilitySystem 是否为空，与契约 [C-Runtime-4a] 不一致
   - 修复：
     - ValidateDependencies 中添加 AbilitySystem 空值检查
     - Awake 时检测到为空：Warning（一次性）+ `hasError=true`
     - SetAbilitySystem 注入后重新校验：
       - 所有必需依赖满足：`InteractionEnabled=true`
       - 其他依赖仍缺失：`InteractionEnabled` 保持 false
   - 契约遵守：符合 [C-Runtime-4a] 必需依赖要求（Inventory/Damageable/AbilitySystem）

##### Phase 4 端到端集成（2025-12-25）

**问题**：Phase 4 缺少关键集成调用点，导致拾取流程无法运行

**缺失的集成**：
1. **PlayerInventory.Initialize()** 无调用点
   - PlayerInventory.cs:119 定义了 Initialize 方法
   - TryPickup 在 line 146 检查未初始化会直接返回 Failed_NotSupported
   - 导致所有拾取操作失败

2. **PlayerContext.SetAbilitySystem()** 无调用点
   - PlayerContext 在 Awake 时检测到 AbilitySystem 为空设置 InteractionEnabled=false
   - ItemPickup 在 line 123 门控检查被拦截
   - 导致拾取逻辑根本不执行

**修复内容**：

**PlayerController 端到端集成**（Assets/Scripts/PlayerController.cs:546-586）
- 位置：BuildAbilitySystem() 方法末尾
- 集成时机：Awake → BuildAbilitySystem() → 集成调用

**1. 注入 AbilitySystem 到 PlayerContext**
```csharp
PlayerContext playerContext = GetComponent<PlayerContext>();
if (playerContext != null)
{
    playerContext.SetAbilitySystem(abilitySystem);
    Debug.Log("已注入 AbilitySystem 到 PlayerContext");
}
```
- 效果：InteractionEnabled 从 false → true（如其他必需依赖满足）
- 闭环：ItemPickup 门控检查通过，可以执行拾取逻辑

**2. 初始化 PlayerInventory**
```csharp
PlayerInventory inventory = GetComponent<PlayerInventory>();
if (inventory != null)
{
    // 加载 ItemCatalog
    var itemCatalog = Resources.Load<ItemCatalog>("Config/ItemCatalog");

    // 创建 CastleDbService 实例
    var castleDbService = new CastleDbService();
    castleDbService.SetItemCatalog(itemCatalog);

    // 加载 GameplayConfig（可选）
    var gameplayConfig = Resources.Load<GameplayConfig>("Config/GameplayConfig");

    // 初始化 PlayerInventory
    inventory.Initialize(castleDbService, gameplayConfig);
}
```
- 效果：PlayerInventory._initialized = true
- 闭环：TryPickup 通过初始化检查，可以处理拾取请求

**端到端流程验证**：
1. Unity 启动 → PlayerController.Awake() → BuildAbilitySystem()
2. BuildAbilitySystem() 创建 AbilitySystem → 注入到 PlayerContext
3. PlayerContext.SetAbilitySystem() → InteractionEnabled=true ✅
4. BuildAbilitySystem() 加载 ItemCatalog → 创建 CastleDbService → 初始化 PlayerInventory
5. PlayerInventory.Initialize() → _initialized=true ✅
6. ItemPickup.OnTriggerEnter2D → GetComponentInParent<PlayerContext>()
7. 门控检查：InteractionEnabled=true ✅ → 继续
8. TryPickup → _initialized=true ✅ → 处理拾取逻辑 ✅

##### Phase 5: AbilitySystem Enable/Disable 队列化机制（已完成，2025-12-25）

**目标**：实现运行时动态控制能力启停，支持 PlayerEquipmentController 的槽位映射

**契约**：[C-Runtime-3] Enable/Disable 队列化

**实现内容**：

1. **IPlayerAbility 接口扩展**
   - 位置：`Scripts/Player/IPlayerAbility.cs`
   - 新增属性：`string AbilityId { get; }`（能力唯一标识符，用于查询和控制）
   - 修改属性：`bool Enabled { get; set; }`（从只读改为可设置，支持 FlushPendingChanges 修改）
   - 文档更新：注释明确 Phase 5 运行时动态修改语义

2. **所有能力实现更新**（5 个文件）
   - DefaultMoveAbility / DefaultRunAbility / DefaultJumpAbility / DefaultAttackAbility / DefaultRangedAttackAbility
   - 构造函数签名：从 `(PlayerController, int priority, bool enabled)` 改为 `(PlayerController, string abilityId, int priority, bool enabled)`
   - 属性添加：`public string AbilityId { get; private set; }`
   - Enabled 修改：从 `{ get; private set; }` 改为 `{ get; set; }`

3. **AbilityRegistry 工厂更新**
   - 位置：`Scripts/Player/AbilityRegistry.cs:72-92`
   - 所有 CreateAbility 调用现在传入 `id` 参数：`new DefaultMoveAbility(playerController, id, priority, enabled)`

4. **AbilitySystem 队列化机制**
   - 位置：`Scripts/Player/AbilitySystem.cs`
   - 新增字段：
     - `Dictionary<string, IPlayerAbility> abilityById`：能力 ID 到实例映射（用于快速查找）
     - `List<(string abilityId, bool enabled)> pendingQueue`：待处理队列（保留写入顺序）
     - `Dictionary<string, bool> pendingLast`：同帧去重记录最后状态
     - `HashSet<string> loggedErrors`：一次性日志去重（abilityId 不存在）

5. **SetAbilityEnabled API**
   - 签名：`bool SetAbilityEnabled(string abilityId, bool enabled)`
   - 逻辑：
     1. abilityId 不存在：Error（一次性）+ 返回 false（不入队）
     2. 状态未变化（ability.Enabled == enabled）：返回 true（不记录，不入队）
     3. 状态变化：追加到 pendingQueue，写入 pendingLast，返回 true
   - 队列化保证：同帧多次调用同一 abilityId，只保留最后状态

6. **IsAbilityEnabled API**
   - 签名：`bool IsAbilityEnabled(string abilityId)`
   - 逻辑：查询 abilityById[abilityId].Enabled，不存在返回 false
   - 日志策略：默认不输出日志（避免噪音，可选启用注释代码用于调试）

7. **FlushPendingChanges 刷新机制**
   - 签名：`void FlushPendingChanges()`
   - 调用时机：PlayerController.LateUpdate()（每帧统一应用）
   - 逻辑：
     1. 遍历 pendingQueue，按写入顺序处理
     2. 仅当 `pendingLast[abilityId] == enabled` 时应用（保留最终写入间相对顺序）
     3. 应用状态：`abilityById[abilityId].Enabled = enabled`
     4. 清空队列：`pendingQueue.Clear()` + `pendingLast.Clear()`
   - 日志：输出应用变更数量（可选通过 `ABILITY_SYSTEM_DEBUG` 宏控制详细日志）

8. **PlayerController.LateUpdate 集成**
   - 位置：`Scripts/PlayerController.cs:657-672`
   - 新增方法：`private void LateUpdate()`
   - 逻辑：
     ```csharp
     if (usePlayerConfigFromCastleDb && abilitySystem != null)
     {
         abilitySystem.FlushPendingChanges();
     }
     ```
   - 效果：每帧在所有 Update 完成后统一刷新能力状态

9. **RegisterAbility 集成更新**
   - 位置：`Scripts/Player/AbilitySystem.cs:88-92`
   - 新增：注册时同步添加到 `abilityById` 映射
   - 校验：`!string.IsNullOrEmpty(ability.AbilityId)` 检查
   - 日志：输出注册能力的 AbilityId

**队列化语义**：
- 保序：pendingQueue 按 SetAbilityEnabled 调用顺序记录
- 去重：pendingLast 记录同帧最后状态，遍历时跳过非最终状态条目
- 原子：LateUpdate 统一应用，避免 Update/FixedUpdate 中状态不一致
- 节流：状态未变化不入队，减少无效操作

**端到端验证流程**：
1. PlayerController.Awake() → BuildAbilitySystem() → 创建 AbilitySystem 并注册能力（填充 abilityById）
2. PlayerEquipmentController.OnSlotChanged() → 调用 `abilitySystem.SetAbilityEnabled(abilityId, enabled)`
3. 同帧多次调用被去重，仅 pendingLast 保留最终状态
4. PlayerController.LateUpdate() → FlushPendingChanges() 按顺序应用最终状态
5. Dispatch 时检查 `ability.Enabled`，禁用的能力被跳过（AbilitySystem.cs:122-126）

##### Phase 6: PlayerEquipmentController 槽位映射实现（已完成，2025-12-26）

**目标**：将 PlayerInventory 槽位变更映射为 AbilitySystem Enable/Disable 调用

**契约**：[C-Runtime-7] PlayerEquipmentController

**实现内容**：

1. **核心类结构**
   - 位置：`Assets/Scripts/Player/PlayerEquipmentController.cs`
   - 依赖：`ICastleDbService`（查询 Item 定义）、`AbilitySystem`（控制能力启停）、`PlayerInventory`（监听槽位变化）
   - 状态：`_slotAbilityIds[4]`（记录每槽当前启用的 abilityId）
   - 初始化规则：Initialize 只允许调用一次，SyncAllSlotsToAbilities 仅在 Start 或 Bootstrap 回调后执行

2. **Initialize 依赖注入**（line 43-67）
   - 签名：`void Initialize(ICastleDbService items, AbilitySystem abilitySystem, PlayerInventory inventory)`
   - 校验：参数非空检查、重复初始化检查
   - 订阅：`_inventory.OnAbilitySlotChanged += OnSlotChanged`
   - 集成点：`PlayerController.BuildAbilitySystem():581-592`（Phase 4 端到端集成之后）

3. **槽位映射核心逻辑**（line 83-112）
   - 事件处理：`OnSlotChanged(int slot, string oldItemId, string newItemId)`
   - itemId → abilityId 解析：通过 `CastleDbService.TryGetItem` 查询 `ItemDefinition.abilityId`
   - 执行顺序（硬契约）：
     1. 若 `oldAbilityId` 非空：`AbilitySystem.SetAbilityEnabled(oldAbilityId, false)`
     2. 若 `newAbilityId` 非空：`AbilitySystem.SetAbilityEnabled(newAbilityId, true)`
   - 顺序保证：先 Disable 后 Enable，确保 pendingQueue 保留替换顺序

4. **GetAbilityIdFromItem 查询与容错**（line 117-162）
   - 空值处理：`itemId` 为空返回 null
   - TryGetItem 失败：Warning（一次性，按 itemId 去重），返回 null（不修改 Inventory 数据）
   - itemType 校验：必须是 `ItemType.Ability`，否则 Warning + 返回 null
   - abilityId 校验：必须非空，否则 Warning + 返回 null
   - 一次性日志去重：`HashSet<string> _loggedWarnings`（按 `"EquipmentController_{错误类型}_{itemId}"` 去重）

5. **初始同步 SyncAllSlotsToAbilities**（line 170-198）
   - 调用时机：Start() 方法中（line 204-214）
   - 逻辑：遍历所有槽位（0~3），读取 `inventory.GetAbilityItemId(slot)`，解析 abilityId 并启用
   - 校验：未初始化时 Error 并返回
   - 记录：更新 `_slotAbilityIds[slot]` 本地缓存
   - 效果：初始能力启用通过 pendingQueue 在 LateUpdate 统一生效

6. **PlayerController 集成**（line 581-592）
   ```csharp
   PlayerEquipmentController equipmentController = GetComponent<PlayerEquipmentController>();
   if (equipmentController != null)
   {
       // 注入依赖：CastleDbService, AbilitySystem, PlayerInventory
       equipmentController.Initialize(castleDbService, abilitySystem, inventory);
       Debug.Log("[PlayerController] 已初始化 PlayerEquipmentController");
   }
   ```
   - 位置：PlayerInventory.Initialize 之后（确保依赖满足）
   - 可选性：EquipmentController 缺失时 Warning（组件为可选）

7. **PlayerContext 字段支持**
   - 位置：`Scripts/Player/PlayerContext.cs:45,86`
   - 字段：`public PlayerEquipmentController EquipmentController { get; private set; }`
   - 装配：`EquipmentController = GetComponent<PlayerEquipmentController>()`（可选依赖）

**端到端验证流程**：
```
1. PlayerController.Awake():
   - BuildAbilitySystem() → 创建 AbilitySystem + 注册能力
   - Initialize PlayerInventory
   - Initialize PlayerEquipmentController → 订阅 OnAbilitySlotChanged ✅

2. PlayerEquipmentController.Start():
   - SyncAllSlotsToAbilities() → 初始槽位同步（若预置数据则启用对应能力）

3. 拾取 Ability Pickup:
   - Inventory.TryPickup → Success
   - OnAbilitySlotChanged(slot, null, "ability_dash")
   - EquipmentController.OnSlotChanged → SetAbilityEnabled("BasicRun", true) ✅

4. LateUpdate:
   - AbilitySystem.FlushPendingChanges() → BasicRun.Enabled = true ✅

5. Dispatch:
   - BasicRun.Enabled=true → OnMove 被调用 ✅
```

**关键改进**：
- ✅ 槽位映射唯一责任主体：EquipmentController 是唯一修改 AbilitySystem Enabled 状态的地方
- ✅ 事件驱动架构：订阅 Inventory 事件，而非轮询
- ✅ 错误容错：TryGetItem 失败/itemType 不符时 Warning 不阻断，视为槽位无能力
- ✅ 执行顺序保证：先 Disable 后 Enable，利用 pendingQueue 保序
- ✅ 一次性日志去重：避免重复 Warning 噪音

**DoD（Phase 6 完成标准）**：
- ✅ PlayerEquipmentController 实现完整（Initialize/OnSlotChanged/SyncAllSlotsToAbilities）
- ✅ 订阅 PlayerInventory.OnAbilitySlotChanged 事件
- ✅ 通过 CastleDbService.TryGetItem 解析 itemId → abilityId
- ✅ 执行顺序正确：先 Disable 旧能力，再 Enable 新能力
- ✅ TryGetItem 失败策略：Warning 一次性，视为槽位无能力
- ✅ 初始同步在 Start 时执行（或 Bootstrap 回调）
- ✅ GameBootstrap 创建完成（符合契约 [C-Runtime-0]）
- ✅ PlayerController 清理完成（仅保留 AbilitySystem 创建）
- ⬜ Unity 场景配置：添加 GameBootstrap GameObject + 配置 player 字段（**待手动操作**）
- ⬜ Script Execution Order 配置：PlayerController(-100), GameBootstrap(0)（**待手动操作**）
- ⬜ Unity PlayMode 验证：日志输出符合预期顺序（**待场景配置后验证**）
- ⬜ Unity PlayMode 验证：拾取触发能力启用（**待场景配置后验证**）
- ⬜ Unity PlayMode 验证：替换触发能力启停（**待场景配置后验证**）

**配置文档**：
- `Logs/NotesLog/CodexProjectLogs/Phase6验证清单.md`（原验证清单）
- `Logs/NotesLog/CodexProjectLogs/Phase6架构修正-GameBootstrap集成.md`（架构修正说明）
- `Logs/NotesLog/CodexProjectLogs/Phase6场景配置指南.md`（**Unity Editor 必需操作清单**）

**关键待办**（Unity Editor 手动操作）：
1. 在 TestEnemy.unity 添加 GameBootstrap GameObject（如需要）
2. 配置 GameBootstrap.player 字段（拖拽 Player）
3. 设置 Script Execution Order（PlayerController -100, GameBootstrap 0）
4. 保存场景并测试运行

##### Phase 7: HUD 数据驱动呈现层（已完成，2025-12-26）

**目标**：实现 HUD 实时更新系统，订阅 Inventory/Damageable 事件，禁止轮询

**契约**：[C-Runtime-6] HUD + [C-UI-1] HudBindingAsset/HudRefs

**实现内容**：

1. **Damageable OnHealthChanged 事件**
   - 位置：`Assets/Scripts/Damageable.cs:28-95`
   - 新增事件：`event Action<int, int> OnHealthChanged; // (current, max)`
   - 新增属性：`public int CurrentHealth => Mathf.RoundToInt(_health);`
   - Health setter 修改：Health 变化时触发事件，clamp 至 0..max，仅实际变化时触发
   - Configure 方法：设置 Health 会触发初始事件

2. **HudBindingAsset ScriptableObject**
   - 位置：`Assets/Scripts/Data/HudBindingAsset.cs`
   - 资源路径（硬契约）：`Assets/Resources/Config/HudBinding.asset`
   - 字段：`GameObject hudPrefab`（必需，指向 HUDCanvas.prefab）
   - 生成方式：由 HUD Quick Config 工具（Phase 10）生成/覆盖
   - Runtime 使用：GameBootstrap 从 Resources.Load 加载并实例化

3. **HudRefs MonoBehaviour**
   - 位置：`Assets/Scripts/UI/HudRefs.cs`
   - 挂载位置：HUDCanvas 根节点（prefab）
   - 字段：
     - `Image[] abilitySlotIcons`（长度 4，索引 0~3 映射到 Slot_0~3）
     - `TMP_Text potionCountText`（血瓶计数）
     - `Image healthFill`（血条 fillAmount）
     - `Image energyFill`（能量条，可选，0.4 版本隐藏）
     - `GameObject abilityReplacePanelRoot`（替换面板根，默认 inactive）
   - OnValidate：校验引用完整性（Editor-only）

4. **HudPresenter 核心实现**
   - 位置：`Assets/Scripts/UI/HudPresenter.cs`
   - 依赖注入：`Initialize(ICastleDbService items, HudRefs refs, PlayerInventory inv, Damageable dmg)`
   - 初始化规则：
     - 只允许调用一次，重复调用 Error 并忽略
     - 完成事件订阅：`OnAbilitySlotChanged` / `OnPotionCountChanged` / `OnHealthChanged`
     - 立即执行 `RefreshAll()`（初始刷新）
   - OnDestroy：取消订阅，避免内存泄漏

5. **Sprite 图标缓存系统**
   - 位置：`HudPresenter.cs:GetSprite()`
   - 对外接口：`public Sprite GetSprite(string iconPath)`（Phase 8 ReplaceController 复用）
   - 缓存策略：
     - `Dictionary<string, Sprite> _spriteCache`（失败缓存 null）
     - `Resources.Load<Sprite>(iconPath)` 只加载一次
     - `HashSet<string> _missingSpriteWarned`（缺失路径仅 Warning 一次）
   - iconPath 为空/加载失败返回 null

6. **HUD 更新逻辑**
   - `RefreshAll()`：初始刷新（Initialize 内调用）
     - 遍历 0~3 槽，读取 `inv.GetAbilityItemId(i)`，加载 icon 并更新
     - `potionCountText.text = inv.PotionCount.ToString()`
     - `UpdateHealthInternal(dmg.CurrentHealth, Mathf.RoundToInt(dmg.MaxHealth))`
   - `UpdateAbilitySlot(slot, itemId)`：
     - itemId 为空：禁用 `image.enabled = false`
     - itemId 非空：TryGetItem 获取 icon，加载 Sprite 并设置
   - `UpdatePotionCountInternal(count)`：更新血瓶计数文本
   - `UpdateHealthInternal(current, max)`：
     - max <= 0：fillAmount = 0
     - 否则：`fillAmount = Mathf.Clamp01((float)current / max)`
     - current 超出范围：Warning + clamp 至 0..max

7. **GameBootstrap 集成（Step 3-4）**
   - 位置：`Assets/Scripts/Gameplay/GameBootstrap.cs:195-280`
   - Step 3: `LoadHudBinding()`
     - 加载 `HudBinding.asset`，验证 `hudPrefab` 非空
     - 若 `hudRootOverride` 存在则复用，否则实例化 `hudPrefab`
   - Step 4: `InitializeHudPresenter()`
     - 从 HUD 实例获取 `HudPresenter` 和 `HudRefs`
     - 验证 `player.Inventory` / `player.Damageable` 非空
     - 调用 `hudPresenter.Initialize(_castleDbService, hudRefs, player.Inventory, player.Damageable)`
   - 日志计数：更新为 `[1/5]...[5/5]`（包含 HUD 步骤）

**端到端验证流程**：
```
1. GameBootstrap.Awake():
   - LoadItemCatalog() → 创建 CastleDbService ✅
   - InitializePlayerInventory() → 订阅事件准备 ✅
   - LoadHudBinding() → 实例化/复用 HUD ✅
   - InitializeHudPresenter() → 订阅 Inventory/Damageable 事件 + RefreshAll() ✅

2. HudPresenter.RefreshAll():
   - 遍历 Ability 槽 → 加载 icon Sprite → 更新 UI ✅
   - 更新血瓶计数 → potionCountText.text ✅
   - 更新血条 → healthFill.fillAmount ✅

3. 拾取 Ability Pickup:
   - Inventory.TryPickup → OnAbilitySlotChanged(slot, null, itemId)
   - HudPresenter.OnAbilitySlotChanged → UpdateAbilitySlot() ✅
   - UI 显示新 Ability 图标 ✅

4. 玩家受击:
   - Damageable.Hit → Health setter → OnHealthChanged(current, max)
   - HudPresenter.OnHealthChanged → UpdateHealthInternal() ✅
   - 血条 fillAmount 实时更新 ✅
```

**关键改进**：
- ✅ 事件驱动：订阅 Inventory/Damageable 事件，禁止轮询
- ✅ 唯一 Sprite 缓存：GetSprite 公开接口，Phase 8 ReplaceController 复用
- ✅ 初始刷新：Initialize 内立即 RefreshAll()，避免 HUD 空白帧
- ✅ 错误容错：TryGetItem 失败/icon 加载失败时禁用图标，不阻断其他槽位
- ✅ 一次性日志去重：缺失 Sprite 仅 Warning 一次

**DoD（Phase 7 完成标准）**：
- ✅ Damageable.OnHealthChanged 事件完整实现
- ✅ HudBindingAsset / HudRefs / HudPresenter 核心类创建
- ✅ Sprite 图标缓存系统（GetSprite 对外接口）
- ✅ HUD 更新逻辑（UpdateAbilitySlots/UpdatePotionCount/UpdateHealth）
- ✅ GameBootstrap Step 3-4 集成完成
- ✅ 事件订阅与初始刷新符合契约
- ⬜ HUD Quick Config 工具（Phase 10 实现）
- ⬜ Unity Prefab 创建：HUDCanvas.prefab（手动或工具生成）
- ⬜ HudBinding.asset 创建并回填 hudPrefab（手动或工具生成）
- ⬜ Unity PlayMode 验证：HUD 初始显示 + 事件更新

**待完成（Phase 10 工具支持）**：
- HUD Quick Config 工具：Create Template / AutoBind / Validate
- 自动生成 HUDCanvas.prefab（符合 [C-UI-1]/[C-UI-2] 节点结构）
- 自动绑定 HudRefs 字段（按固定路径）
- 生成 HudBinding.asset 并回填 hudPrefab

##### Phase 7: HUD 数据驱动呈现层（已完成，2025-12-26）

**目标**：实现 HUD 实时更新系统，订阅 Inventory/Damageable 事件，禁止轮询

**契约**：[C-Runtime-6] HUD + [C-UI-1] HudBindingAsset/HudRefs

**实现内容**：

1. **Damageable OnHealthChanged 事件**
   - 位置：`Assets/Scripts/Damageable.cs:28-95`
   - 新增事件：`event Action<int, int> OnHealthChanged; // (current, max)`
   - 新增属性：`public int CurrentHealth => Mathf.RoundToInt(_health);`
   - Health setter 修改：Health 变化时触发事件，clamp 至 0..max，仅实际变化时触发
   - Configure 方法：设置 Health 会触发初始事件

2. **HudBindingAsset ScriptableObject**
   - 位置：`Assets/Scripts/Data/HudBindingAsset.cs`
   - 资源路径（硬契约）：`Assets/Resources/Config/HudBinding.asset`
   - 字段：`GameObject hudPrefab`（必需，指向 HUDCanvas.prefab）
   - 生成方式：由 HUD Quick Config 工具（Phase 10）生成/覆盖
   - Runtime 使用：GameBootstrap 从 Resources.Load 加载并实例化

3. **HudRefs MonoBehaviour**
   - 位置：`Assets/Scripts/UI/HudRefs.cs`
   - 挂载位置：HUDCanvas 根节点（prefab）
   - 字段：
     - `Image[] abilitySlotIcons`（长度 4，索引 0~3 映射到 Slot_0~3）
     - `TMP_Text potionCountText`（血瓶计数）
     - `Image healthFill`（血条 fillAmount）
     - `Image energyFill`（能量条，可选，0.4 版本隐藏）
     - `GameObject abilityReplacePanelRoot`（替换面板根，默认 inactive）
   - OnValidate：校验引用完整性（Editor-only）

4. **HudPresenter 核心实现**
   - 位置：`Assets/Scripts/UI/HudPresenter.cs`
   - 依赖注入：`Initialize(ICastleDbService items, HudRefs refs, PlayerInventory inv, Damageable dmg)`
   - 初始化规则：
     - 只允许调用一次，重复调用 Error 并忽略
     - 完成事件订阅：`OnAbilitySlotChanged` / `OnPotionCountChanged` / `OnHealthChanged`
     - 立即执行 `RefreshAll()`（初始刷新）
   - OnDestroy：取消订阅，避免内存泄漏

5. **Sprite 图标缓存系统**
   - 位置：`HudPresenter.cs:GetSprite()`
   - 对外接口：`public Sprite GetSprite(string iconPath)`（Phase 8 ReplaceController 复用）
   - 缓存策略：
     - `Dictionary<string, Sprite> _spriteCache`（失败缓存 null）
     - `Resources.Load<Sprite>(iconPath)` 只加载一次
     - `HashSet<string> _missingSpriteWarned`（缺失路径仅 Warning 一次）
   - iconPath 为空/加载失败返回 null

6. **HUD 更新逻辑**
   - `RefreshAll()`：初始刷新（Initialize 内调用）
     - 遍历 0~3 槽，读取 `inv.GetAbilityItemId(i)`，加载 icon 并更新
     - `potionCountText.text = inv.PotionCount.ToString()`
     - `UpdateHealthInternal(dmg.CurrentHealth, Mathf.RoundToInt(dmg.MaxHealth))`
   - `UpdateAbilitySlot(slot, itemId)`：
     - itemId 为空：禁用 `image.enabled = false`
     - itemId 非空：TryGetItem 获取 icon，加载 Sprite 并设置
   - `UpdatePotionCountInternal(count)`：更新血瓶计数文本
   - `UpdateHealthInternal(current, max)`：
     - max <= 0：fillAmount = 0
     - 否则：`fillAmount = Mathf.Clamp01((float)current / max)`
     - current 超出范围：Warning + clamp 至 0..max

7. **GameBootstrap 集成（Step 3-4）**
   - 位置：`Assets/Scripts/Gameplay/GameBootstrap.cs:195-280`
   - Step 3: `LoadHudBinding()`
     - 加载 `HudBinding.asset`，验证 `hudPrefab` 非空
     - 若 `hudRootOverride` 存在则复用，否则实例化 `hudPrefab`
   - Step 4: `InitializeHudPresenter()`
     - 从 HUD 实例获取 `HudPresenter` 和 `HudRefs`
     - 验证 `player.Inventory` / `player.Damageable` 非空
     - 调用 `hudPresenter.Initialize(_castleDbService, hudRefs, player.Inventory, player.Damageable)`
   - 日志计数：更新为 `[1/5]...[5/5]`（包含 HUD 步骤）

**关键改进**：
- ✅ 事件驱动：订阅 Inventory/Damageable 事件，禁止轮询
- ✅ 唯一 Sprite 缓存：GetSprite 公开接口，Phase 8 ReplaceController 复用
- ✅ 初始刷新：Initialize 内立即 RefreshAll()，避免 HUD 空白帧
- ✅ 错误容错：TryGetItem 失败/icon 加载失败时禁用图标，不阻断其他槽位
- ✅ 一次性日志去重：缺失 Sprite 仅 Warning 一次

**DoD（Phase 7 完成标准）**：
- ✅ Damageable.OnHealthChanged 事件完整实现
- ✅ HudBindingAsset / HudRefs / HudPresenter 核心类创建
- ✅ Sprite 图标缓存系统（GetSprite 对外接口）
- ✅ HUD 更新逻辑（UpdateAbilitySlots/UpdatePotionCount/UpdateHealth）
- ✅ GameBootstrap Step 3-4 集成完成
- ✅ 事件订阅与初始刷新符合契约
- ✅ **HudBinding.asset 已生成**（2025-12-26 通过 Phase 10 工具）
- ✅ **HUDCanvas.prefab 已生成**（2025-12-26 通过 Phase 10 工具）

##### Phase 10: HUD Quick Config 工具（已完成，2025-12-26，契约细节修复 2025-12-27）

**目标**：实现 Create Template / AutoBind / Validate 工具，生成 HUD 必需产物

**契约**：[C-Tool-1] HUD Quick Config

**实现内容**：

1. **HudQuickConfigWindow 编辑器窗口**
   - 位置：`Assets/Scripts/UI/Editor/HudQuickConfigWindow.cs`
   - 菜单路径：`Tools → UI → HUD Quick Config`
   - 功能按钮：Create Template / AutoBind / Validate

2. **Create Template 功能**
   - 生成路径（硬契约）：
     - `Assets/Resources/Prefabs/UI/HUDCanvas.prefab`（HUD 模板）
     - `Assets/Resources/Config/HudBinding.asset`（资源入口）
   - 节点结构：完全符合 [C-UI-1]（HUD 基础节点）和 [C-UI-2]（AbilityReplacePanel 结构）
   - Canvas 配置：
     - Render Mode = Screen Space - Overlay
     - Canvas Scaler = Scale With Screen Size (1920x1080)
     - 包含 GraphicRaycaster 组件
   - HUD 层级结构：
     - `BottomLeft/AbilityBar/Slot_0~3/Icon`（Image）
     - `BottomLeft/PotionWidget/CountText`（TextMeshProUGUI）
     - `BottomCenter/HealthBar/Fill`（Image fillAmount）
     - `BottomCenter/EnergyBar/Fill`（Image fillAmount，默认隐藏）
     - `Overlay/AbilityReplacePanel`（默认隐藏，包含 [C-UI-2] 完整结构）
   - 自动回填：HudBinding.hudPrefab 自动指向生成的 HUDCanvas.prefab
   - 自动绑定：生成时立即绑定 HudRefs 所有字段

3. **AutoBind 功能**
   - 按固定路径自动绑定 HudRefs 字段：
     - `abilitySlotIcons[0~3]` → `BottomLeft/AbilityBar/Slot_0~3/Icon`
     - `potionCountText` → `BottomLeft/PotionWidget/CountText`
     - `healthFill` → `BottomCenter/HealthBar/Fill`
     - `energyFill` → `BottomCenter/EnergyBar/Fill`
     - `abilityReplacePanelRoot` → `Overlay/AbilityReplacePanel`
   - **契约细节修复（2025-12-27）**：添加 `HudBinding.hudPrefab` 非空校验
     - 检查 `binding == null || binding.hudPrefab == null`
     - 为空则 Error + 对话框提示用户先执行 Create Template
   - 错误处理：缺失节点或类型不符直接 Error（不允许静默跳过）

4. **Validate 功能**
   - 校验 HudRefs 引用完整性：
     - abilitySlotIcons 长度必须为 4
     - 所有必需字段非空
   - 校验 [C-UI-2] AbilityReplacePanel 节点：
     - `Pending/Icon`（Image）
     - `Pending/NameText`（TextMeshProUGUI）
     - `Slots/Slot_0~3/Icon`（Image）
     - `Slots/Slot_0~3/Highlight`（Transform，验证 GameObject 存在）
   - **契约细节修复（2025-12-27）**：错误消息格式符合契约 [C-Tool-1]
     - 缺节点：`[HUDQuickConfig][ERROR] Missing node: <path>`
     - 缺 ReplacePanelRoot：`[HUDQuickConfig][ERROR] Missing ReplacePanelRoot: <path>`
     - 类型不符：`[HUDQuickConfig][ERROR] Expected <Image/TMP_Text> at <path>, got <type>`
     - 槽位数量：`[HUDQuickConfig][ERROR] Ability slot icon count must be 4, got <n>`

5. **产物验证**（2025-12-26）
   - ✅ `Assets/Resources/Config/HudBinding.asset` 存在（72,792 字节）
   - ✅ `Assets/Resources/Prefabs/UI/HUDCanvas.prefab` 存在（491 字节）
   - ✅ HudBinding.hudPrefab 引用已回填
   - ✅ HudRefs 所有字段已绑定
   - ✅ Validate 通过（所有节点完整）

**关键改进**：
- ✅ 一键生成：Create Template 完整生成所有必需产物
- ✅ 契约遵守：节点结构 100% 符合 [C-UI-1]/[C-UI-2]
- ✅ 错误诊断：详细的错误消息和日志前缀 `[HUDQuickConfig][ERROR]`（Validate）
- ✅ 原子操作：生成失败不留半成品，成功后立即保存资产
- ✅ **契约细节**：AutoBind 校验 hudPrefab 非空 + Validate 日志格式符合契约

**DoD（Phase 10 完成标准）**：
- ✅ HudQuickConfigWindow 编辑器窗口创建
- ✅ Create Template 功能完整实现
- ✅ AutoBind 功能完整实现（**含 hudPrefab 非空校验**）
- ✅ Validate 功能完整实现（**含契约日志格式**）
- ✅ 产物已生成并验证通过
- ✅ Unity Editor 菜单集成（Tools → UI → HUD Quick Config）
- ✅ 编译错误已修复（GameObject → Transform 泛型约束）
- ✅ **契约细节完全对齐**（2025-12-27）

**端到端验证流程**：
```
1. Unity 菜单 → Tools → UI → HUD Quick Config
2. 点击 "Create Template" → 成功生成 HUDCanvas.prefab 和 HudBinding.asset
3. 点击 "Validate" → 通过所有节点校验 + 日志格式符合契约
4. GameBootstrap.Awake() → LoadHudBinding() → 成功加载
5. GameBootstrap → InitializeHudPresenter() → 订阅事件并 RefreshAll()
6. Play Mode → HUD 正确显示（能力槽/血瓶/血条）
```

##### Phase 6 场景配置补充（2025-12-26）

**遗留问题**：Script Execution Order 未设置，导致 GameBootstrap.Awake 先于 PlayerController.Awake 执行

**必需操作**（Unity Editor）：
1. **设置 Script Execution Order**
   - Unity 菜单：`Edit → Project Settings → Script Execution Order`
   - 配置：
     ```
     PlayerController  → -100  (更早执行)
     GameBootstrap     → 0     (默认顺序)
     ```
   - 原因：GameBootstrap 依赖 PlayerController 创建 AbilitySystem
   - 症状（未设置时）：`[GameBootstrap] PlayerContext.AbilitySystem 为空，中止装配`

2. **场景 GameBootstrap 配置**
   - TestEnemy.unity 添加 GameBootstrap GameObject（如未添加）
   - Inspector 配置 `player` 字段（拖拽 Player GameObject）
   - 保存场景

**参考文档**：`Logs/NotesLog/CodexProjectLogs/Phase6场景配置指南.md`

##### Phase 8: AbilityReplacePanel + ReplaceController 完整实现（已完成，2025-12-26）

**目标**：实现槽满拾取的替换流程，支持用户选择槽位替换能力

**契约**：[C-Runtime-5] ReplaceController + [C-UI-2] AbilityReplacePanel + [C-Input-1] 输入模式切换

**实现内容**：

1. **ReplaceController 完整实现**
   - 位置：`Assets/Scripts/UI/ReplaceController.cs`
   - 状态机：Idle → Selecting → Confirm/Cancel → Idle
   - 依赖注入：`Initialize(ICastleDbService items, HudRefs refs, PlayerContext ctx, HudPresenter hud)`
   - UI 节点缓存：Initialize 时按固定路径查找并缓存（[C-UI-2] 节点结构）

2. **BeginReplace 流程**（契约 [C-Runtime-5a]）
   - 参数校验：`ctx.sourcePickup` 必须非空（先于状态检查）
   - 状态检查：仅 Idle 可调用，非 Idle 解锁新 pickup
   - 未初始化降级：直接解锁 pickup，不进入 Selecting
   - 进入 Selecting：切换输入 → 渲染面板 → 显示面板
   - 切换失败：自动 CancelInternal(unlock=true)

3. **输入映射**（契约 [C-Input-1]）
   - 切换到 UI ActionMap：`SwitchToUIInput()` 记录 `_prevActionMap` 并切换
   - 键位-槽位映射（硬契约）：1→slot0, 2→slot1, 3→slot2, 4→slot3
   - 取消键：ESC → Cancel()
   - 输入恢复：`RestoreInput()` 恢复到 `_prevActionMap`（任何离开 Selecting 路径都必须恢复）

4. **UI 渲染逻辑**
   - `RenderPanel()`：渲染 Pending（待入槽物品）+ Slots（当前 4 槽）
   - `RenderPendingItem()`：通过 `CastleDbService.TryGetItem` 查询 ItemDefinition，复用 `HudPresenter.GetSprite` 加载图标
   - `RenderSlot()`：读取 `Inventory.GetAbilityItemId(slot)`，加载图标并显示
   - 空槽处理：`image.enabled = false`
   - 查询失败容错：禁用图标，不阻断其他槽位

5. **用户输入处理**
   - `Update()` 监听键盘输入（仅 Selecting 状态）
   - `OnSlotSelected(slotIndex)`：高亮选中槽位（Highlight active），执行 Confirm
   - `OnCancelPressed()`：调用 Cancel()

6. **Confirm/Cancel 流程**（契约 [C-Runtime-5] 硬步骤）
   - Confirm 流程：
     1. ValidatePending：校验上下文有效性
     2. TryGetItem：校验待入槽物品存在
     3. 校验 itemType 必须是 Ability
     4. EquipAbilityItemToSlot：尝试装备到指定槽位
     5. 销毁拾取物：`Destroy(sourcePickup.gameObject)`
     6. ExitSelecting：统一清理
   - Cancel 流程：`CancelInternal(unlock=true)`
   - CancelInternal：解锁 pickup（可选）→ ExitSelecting
   - ExitSelecting（统一清理）：恢复输入 → 隐藏面板 → 清空 pending → 状态回 Idle

7. **GameBootstrap 集成**（Step 5）
   - 位置：`Assets/Scripts/Gameplay/GameBootstrap.cs:292-323`
   - 验证 HudPresenter 已初始化（Phase 8 依赖）
   - 从 PlayerContext 获取 ReplaceController（可选依赖）
   - 获取 HudRefs（已在 Step 4 验证）
   - 调用 `replaceController.Initialize(_castleDbService, hudRefs, player, _hudPresenter)`
   - 日志计数：更新为 `[1/6]...[6/6]`（包含 ReplaceController 步骤）

**关键改进**：
- ✅ 状态机完整：Idle → Selecting → Confirm/Cancel → Idle
- ✅ 输入切换安全：记录 prevActionMap，任何退出路径都恢复
- ✅ UI 渲染正确：复用 HudPresenter.GetSprite，避免重复 IO
- ✅ 错误容错完整：ValidatePending/TryGetItem 失败走 CancelInternal
- ✅ ExitSelecting 统一清理：避免重复逻辑，保证状态一致性

**DoD（Phase 8 完成标准）**：
- ✅ ReplaceController 完整实现（Initialize/BeginReplace/Confirm/Cancel）
- ✅ 状态机符合契约 [C-Runtime-5]
- ✅ 输入映射符合契约 [C-Input-1]（ActionMap 切换/恢复）
- ✅ UI 渲染符合契约 [C-Runtime-5]（复用 HudPresenter.GetSprite）
- ✅ UI 节点缓存符合契约 [C-UI-2]（固定路径查找）
- ✅ GameBootstrap Step 5 集成完成
- ✅ ExitSelecting 统一清理（禁止重复手写清理逻辑）
- ✅ CancelInternal 作为失败路径唯一出口
- ⬜ Unity PlayMode 验证：槽满拾取触发替换面板
- ⬜ Unity PlayMode 验证：1~4 键选择槽位，ESC 取消
- ⬜ Unity PlayMode 验证：Confirm 成功装备并销毁 pickup
- ⬜ Unity PlayMode 验证：Cancel 解锁 pickup 并恢复输入

**端到端验证流程**：
```
1. 预置 4 个 Ability 到 Inventory 槽位（槽满状态）
2. 拾取第 5 个 Ability Pickup
   → ItemPickup.TryPickup() → RequireReplace
   → SetLocked(true)
   → ReplaceController.BeginReplace()
   → 进入 Selecting 状态
   → 切换到 UI ActionMap ✅
   → 渲染面板（Pending + Slots） ✅
   → 显示面板 ✅
3. 用户按 1~4 键选择槽位
   → OnSlotSelected() → 高亮槽位 ✅
   → Confirm() → EquipAbilityItemToSlot() ✅
   → Destroy(pickup) ✅
   → ExitSelecting() → 恢复输入 ✅ → 隐藏面板 ✅
4. 或者用户按 ESC 取消
   → OnCancelPressed() → Cancel()
   → CancelInternal(unlock=true) → 解锁 pickup ✅
   → ExitSelecting() → 恢复输入 ✅ → 隐藏面板 ✅
```

##### Phase 12: CDB Excel 导出（契约 [C-Tool-2]）（已完成，2025-12-29）

**目标**：每次 CastleDB 导入完成后，自动生成可阅读的 Excel 版本，便于人工查看数据

**实现内容**：

1. **CdbImportSettings ScriptableObject**
   - 位置：`Assets/Scripts/Data/CastleDB/Editor/CdbImportSettings.cs`
   - 路径：`Assets/Settings/CdbImportSettings.asset`
   - 配置：`cdbImportRoot`（默认 `Assets/Resources`）
   - 菜单：`Tools → CastleDB → Settings` 打开/创建设置

2. **CdbImportRoot 校验（双层防护）**
   - ImportCoordinator 层：ImportAll/ImportSingleModule 早期校验（步骤 0.5）
   - Exporter 层：ExportToExcel 独立校验（ValidateCdbImportRoot）
   - 失败策略：Error 日志 + 返回 false + 中止导入

3. **Import This File 导出范围**
   - 修改：ImportSingleModule 仅导出目标 .cdb（不导出依赖项）
   - 实现：`ExportModulesToExcel(new List<CdbModuleDescriptor> { targetDescriptor }, TriggerMode.ThisFile)`
   - 效果：右键 Import This File 只生成单个 Excel 文件

4. **Excel 导出触发时机**
   - Import All：步骤 9/10（SaveAssets 之后）
   - Import This File：步骤 10/11（SaveAssets 之后）

5. **输出路径镜像**
   - 基准：CdbImportRoot 配置路径
   - 输出：`ProjectRoot/Docs/cdbExcel/`
   - 映射：`<CdbImportRoot>/Data/Item.cdb` → `Docs/cdbExcel/Data/Item.xlsx`
   - 不安全路径：`__Orphaned/<name>__<hash8>.xlsx`

6. **__Meta Worksheet**
   - Key-Value 区：exportTimeUtc / triggerMode / toolVersion / sourceCdbPathRel / isOrphaned / unityVersion
   - 空行分隔
   - 映射表区：originalSheetName / sanitizedSheetName / finalWorksheetName / rowCount / colCount

7. **测试覆盖**
   - EditMode：`CdbExcelExportTests.cs`（9 个测试用例）
     - Worksheet 名合法化/重名处理
     - 不安全路径检测（__Orphaned）
     - 类型映射与 JSON 字段
     - __Meta worksheet schema
     - 输出路径镜像
     - 原子替换（.tmp.xlsx 机制）
     - Import This File 仅导出目标文件
     - CdbImportRoot 不可访问时失败
   - EditMode：`CdbImportIntegrationTests.cs`（4 个测试用例）
     - ImportAll CdbImportRoot 校验失败
     - ImportSingleModule 仅导出目标文件（集成测试框架）
     - ExportToExcel 独立校验
     - CdbImportRoot 空值降级

**关键改进**：
- ✅ 配置可审查：Settings asset 提交到仓库，默认配置明确
- ✅ 双层防护：Coordinator 早期拦截 + Exporter 独立校验
- ✅ 导出范围正确：Import This File 仅导出目标文件
- ✅ 路径镜像规范：以 CdbImportRoot 为基准，安全路径检测
- ✅ 测试覆盖完整：13 个测试用例覆盖关键路径

**DoD（Phase 12 完成标准）**：
- ✅ ImportCoordinator 提供 CdbImportRoot 配置
- ✅ Import All/This File 成功后触发 Excel 导出
- ✅ 输出到 `ProjectRoot/Docs/cdbExcel/`，路径镜像正确
- ✅ 不安全路径输出到 `__Orphaned/`
- ✅ Worksheet 名合法化，重名追加后缀
- ✅ `__Meta` worksheet 记录完整信息
- ✅ EditMode 测试：非法名/重名/复杂字段校验
- ✅ 导出失败不影响导入结果
- ✅ Settings asset 已提交到仓库
- ✅ 集成测试覆盖 Import 流程

##### Phase 9: Authoring Pipeline（已完成，2025-12-27）

**目标**：实现 Item 从数据制作到场景落地的完整工作流

**契约**：[C-Author-1] Authoring Pipeline

**实现内容**：

1. **Icon 资源规范（硬契约）**
   - 位置：`Assets/Resources/Icons/Items/`（固定目录）
   - Item.cdb icon 路径：`Icons/Items/<name>`（无扩展名）
   - 示例：`Icons/Items/Potion_Red`、`Icons/Items/Ability_Dash`
   - Texture Type：Sprite (2D and UI)

2. **通用拾取物 Prefab 规范**
   - 位置：`Assets/Resources/Prefabs/PickupItem.prefab`
   - 必备组件：
     - `SpriteRenderer`（sprite 由 Inspector 绑定）
     - `Collider2D`（isTrigger=true，BoxCollider2D）
     - `ItemPickup` 组件
   - 世界表现口径：sprite 由 Prefab/Inspector 绑定，运行时不自动设置

3. **ItemPickupEditor 自定义 Inspector**
   - 位置：`Assets/Scripts/Gameplay/Editor/ItemPickupEditor.cs`
   - 核心功能：
     - itemId 下拉选择（来自 ItemCatalog，显示 `id - displayName`）
     - 选中后自动设置 `SpriteRenderer.sprite`（从 icon 路径加载）
     - 防呆校验：
       - `itemType==Ability` 强制 `amount=1`（自动修正）
       - `itemType==Material` 显示 Error（0.4 不支持拾取）
       - `amount <= 0` 自动修正为 1
     - 预览：显示 icon、itemType、abilityId（Ability 类型）
     - 实时验证：
       - itemId 存在性
       - icon 资源可加载性
       - SpriteRenderer.sprite 非空
       - Collider2D.isTrigger 正确性

4. **PickupCreationTool 快速创建工具（可选）**
   - 位置：`Assets/Scripts/Gameplay/Editor/PickupCreationTool.cs`
   - 菜单路径：`GameObject → Create Pickup From Item...`
   - 功能：
     - 弹出窗口选择 Item（下拉列表）
     - 自动创建 GameObject（命名 `Pickup_<itemId>`）
     - 自动添加 SpriteRenderer + Collider2D + ItemPickup
     - 自动绑定 sprite（从 ItemDefinition.icon）
     - 自动设置 amount（Ability=1）
     - Collider 自动匹配 sprite 大小

5. **PickupPrefabGenerator 模板生成工具**
   - 位置：`Assets/Scripts/Gameplay/Editor/PickupPrefabGenerator.cs`
   - 菜单路径：`Tools → Authoring → Generate PickupItem.prefab Template`
   - 功能：
     - 生成通用 PickupItem.prefab 模板
     - 自动创建目录 `Assets/Resources/Prefabs/`
     - 组件配置：SpriteRenderer + BoxCollider2D(isTrigger) + ItemPickup

6. **Item.cdb icon 路径更新**
   - 从 `UI/Items/` 迁移到 `Icons/Items/`（符合契约）
   - 示例更新：
     - `potion_red`: `Icons/Items/Potion_Red`
     - `ability_dash`: `Icons/Items/Ability_Dash`

**关键改进**：
- ✅ Inspector 集成：无需手动填写 itemId，下拉选择即可
- ✅ 自动绑图：选择 Item 后立即加载 sprite 到 SpriteRenderer
- ✅ 防呆校验：Ability amount=1 自动修正，Material 类型 Error 提示
- ✅ 快速创建：右键菜单一键生成拾取物 GameObject
- ✅ 实时验证：Inspector 底部显示所有验证状态（✅/⚠/❌）

**DoD（Phase 9 完成标准）**：
- ✅ Icon 目录规范已建立（`Assets/Resources/Icons/Items/`）
- ✅ Item.cdb icon 路径已更新为 `Icons/Items/`
- ✅ ItemPickupEditor 完整实现（下拉选择 + 自动绑图 + 防呆校验）
- ✅ PickupCreationTool 快捷工具实现（可选功能）
- ✅ PickupPrefabGenerator 模板工具实现
- ⬜ Unity Editor 操作：运行 `Tools → Authoring → Generate PickupItem.prefab Template` 生成 Prefab
- ⬜ Unity Editor 操作：重新导入 Item.cdb（icon 路径已更新）
- ⬜ Unity PlayMode 验证：场景拖入 PickupItem，选择 itemId，运行可拾取

**端到端验证流程**：
```
1. Unity 菜单 → Tools → Authoring → Generate PickupItem.prefab Template
   → 生成 Assets/Resources/Prefabs/PickupItem.prefab ✅

2. Unity 菜单 → GameObject → Create Pickup From Item...
   → 选择 Item → 创建 Pickup GameObject + 自动绑图 ✅

3. 或者：场景拖入 PickupItem.prefab
   → Inspector 下拉选择 itemId → 自动绑定 sprite ✅
   → Validation 显示 ✅ All validations passed

4. Play Mode → 拾取物可交互 → HUD 图标正确显示 ✅
```

##### Phase 11: EditMode/PlayMode 测试（已完成，2025-12-27，编译修正完成）

**目标**：实现核心功能的自动化测试，覆盖关键流程与边界条件

**契约**：[C-Test-1] 测试规格

**编译修正**（2025-12-27）：
1. ✅ ItemDataProviderTests.cs 添加 `using UnityEngine.TestTools;` 和 `using CastleDB.Editor.Providers;`
2. ✅ HudUpdateTests.cs 修改 `Damageable.Configure()` 使用 `DamageableStats` 结构体
3. ✅ CastleDB.Tests.PlayMode.asmdef 添加 Unity.TextMeshPro 引用（GUID: 6055be8ebefd69e48b49212b09b47b2f）

**实现内容**：

1. **ItemDataProviderTests (EditMode)**
   - 位置：`Assets/Tests/EditMode/ItemDataProviderTests.cs`
   - 测试覆盖：
     - ✅ schemaVersion 不匹配检测
     - ✅ providerId 不匹配检测
     - ✅ 重复 id 错误检测
     - ✅ itemType 枚举非法检测
     - ✅ Ability 缺失 abilityId 检测
     - ✅ consumeEffectJson 非 JSON 对象检测
     - ✅ maxStack <= 0 检测
     - ✅ 正常数据导入验证

2. **AbilityPickupTests (PlayMode)**
   - 位置：`Assets/Tests/PlayMode/AbilityPickupTests.cs`
   - 测试覆盖：
     - ✅ Ability 拾取成功入槽
     - ✅ Ability 拾取触发 OnAbilitySlotChanged 事件
     - ✅ 重复 Ability 拾取返回 Failed_AlreadyEquipped
     - ✅ 槽满时返回 RequireReplace（框架）

3. **ConsumablePickupTests (PlayMode)**
   - 位置：`Assets/Tests/PlayMode/ConsumablePickupTests.cs`
   - 测试覆盖：
     - ✅ Consumable 拾取累加计数
     - ✅ Consumable 拾取触发 OnPotionCountChanged 事件
     - ✅ **满仓拾取**：填满后再拾取返回 Failed_NotSupported，事件不触发
     - ✅ Consumable 累加超出上限 clamp 到上限
     - ✅ 多次拾取正确累加

4. **HudUpdateTests (PlayMode)**
   - 位置：`Assets/Tests/PlayMode/HudUpdateTests.cs`
   - 测试覆盖：
     - ✅ **HUD 初始刷新**：预置 Inventory 后启动，HUD 正确显示
     - ✅ Ability 拾取后 HUD 更新（通过事件验证）
     - ✅ Consumable 拾取后 HUD 文本更新
     - ✅ Health 变化后 HUD fillAmount 更新
     - ✅ Health 为 0 时 fillAmount 正确显示
     - ✅ Health 超出范围 clamp 验证

**测试策略**：
- **EditMode**：校验数据导入流程的错误检测能力
- **PlayMode**：验证运行时核心功能与事件驱动机制
- **断言方式**：
  - Inventory：直接访问公开 API（GetAbilityItemId/PotionCount）
  - 事件：通过回调计数器验证触发次数与参数
  - HUD：通过 HudRefs 绑定引用验证 UI 状态

**关键验证**：
- ✅ 满仓 Consumable 拾取：Failed_NotSupported + 事件不触发 + HUD 不变
- ✅ HUD 初始刷新：通过公开 API 预置数据，启动后立即断言
- ✅ 事件驱动：所有 HUD 更新通过事件订阅，禁止轮询
- ✅ 边界条件：Health clamp、Consumable overflow、重复 Ability

**DoD（Phase 11 完成标准）**：
- ✅ ItemDataProvider EditMode 测试创建（8 个测试用例）
- ✅ AbilityPickupTests PlayMode 测试创建（4 个测试用例）
- ✅ ConsumablePickupTests PlayMode 测试创建（5 个测试用例）
- ✅ HudUpdateTests PlayMode 测试创建（6 个测试用例）
- ⬜ Unity Test Runner 运行验证（需在 Unity Editor 中执行）
- ⬜ 修复所有失败用例（如有）

**运行测试**（Unity Editor）：
```
1. Unity 菜单 → Window → General → Test Runner
2. 选择 EditMode 标签 → Run All
3. 选择 PlayMode 标签 → Run All
4. 验证所有测试通过（绿色）
```

**已知限制**：
- EditMode 测试中的 `TryImport` 简化实现，完整导入流程需依赖 ImportCoordinator
- PlayMode 测试环境可能缺少 sprite 资源，部分 HUD 图标验证跳过
- Replace 面板测试未完整实现（需输入模拟，复杂度较高）

##### Phase 12: CDB Excel 导出（契约 [C-Tool-2]）（已完成，2025-12-29）

**目标**：每次 CastleDB 导入完成后，自动生成可阅读的 Excel 版本，便于人工查看数据

**实现内容**：

1. **CdbImportSettings ScriptableObject**
   - 位置：`Assets/Scripts/Data/CastleDB/Editor/CdbImportSettings.cs`
   - 路径：`Assets/Settings/CdbImportSettings.asset`
   - 配置：`cdbImportRoot`（默认 `Assets/Resources`）
   - 菜单：`Tools → CastleDB → Settings` 打开/创建设置

2. **CdbImportRoot 校验（双层防护）**
   - ImportCoordinator 层：ImportAll/ImportSingleModule 早期校验（步骤 0.5）
   - Exporter 层：ExportToExcel 独立校验（ValidateCdbImportRoot）
   - 失败策略：Error 日志 + 返回 false + 中止导入

3. **Import This File 导出范围**
   - 修改：ImportSingleModule 仅导出目标 .cdb（不导出依赖项）
   - 实现：`ExportModulesToExcel(new List<CdbModuleDescriptor> { targetDescriptor }, TriggerMode.ThisFile)`
   - 效果：右键 Import This File 只生成单个 Excel 文件

4. **Excel 导出触发时机**
   - Import All：步骤 9/10（SaveAssets 之后）
   - Import This File：步骤 10/11（SaveAssets 之后）

5. **输出路径镜像**
   - 基准：CdbImportRoot 配置路径
   - 输出：`ProjectRoot/Docs/cdbExcel/`
   - 映射：`<CdbImportRoot>/Data/Item.cdb` → `Docs/cdbExcel/Data/Item.xlsx`
   - 不安全路径：`__Orphaned/<name>__<hash8>.xlsx`

6. **__Meta Worksheet**
   - Key-Value 区：exportTimeUtc / triggerMode / toolVersion / sourceCdbPathRel / isOrphaned / unityVersion
   - 空行分隔
   - 映射表区：originalSheetName / sanitizedSheetName / finalWorksheetName / rowCount / colCount

7. **测试覆盖**
   - EditMode：`CdbExcelExportTests.cs`（9 个测试用例）
     - Worksheet 名合法化/重名处理
     - 不安全路径检测（__Orphaned）
     - 类型映射与 JSON 字段
     - __Meta worksheet schema
     - 输出路径镜像
     - 原子替换（.tmp.xlsx 机制）
     - Import This File 仅导出目标文件
     - CdbImportRoot 不可访问时失败
   - EditMode：`CdbImportIntegrationTests.cs`（4 个测试用例）
     - ImportAll CdbImportRoot 校验失败
     - ImportSingleModule 仅导出目标文件（集成测试框架）
     - ExportToExcel 独立校验
     - CdbImportRoot 空值降级

**关键改进**：
- ✅ 配置可审查：Settings asset 提交到仓库，默认配置明确
- ✅ 双层防护：Coordinator 早期拦截 + Exporter 独立校验
- ✅ 导出范围正确：Import This File 仅导出目标文件
- ✅ 路径镜像规范：以 CdbImportRoot 为基准，安全路径检测
- ✅ 测试覆盖完整：13 个测试用例覆盖关键路径

**DoD（Phase 12 完成标准）**：
- ✅ ImportCoordinator 提供 CdbImportRoot 配置
- ✅ Import All/This File 成功后触发 Excel 导出
- ✅ 输出到 `ProjectRoot/Docs/cdbExcel/`，路径镜像正确
- ✅ 不安全路径输出到 `__Orphaned/`
- ✅ Worksheet 名合法化，重名追加后缀
- ✅ `__Meta` worksheet 记录完整信息
- ✅ EditMode 测试：非法名/重名/复杂字段校验
- ✅ 导出失败不影响导入结果
- ✅ Settings asset 已提交到仓库
- ✅ 集成测试覆盖 Import 流程

**关键改进（0.4）**:
- ✅ 契约先行：所有数据/接口/状态机/错误处理在规划阶段锁定
- ✅ 依赖注入：统一由 GameBootstrap 显式注入，禁止静态单例/FindObjectOfType
- ✅ 事件驱动 HUD：订阅 Inventory/Damageable 事件，禁止轮询
- ✅ 原子拾取流程：RequireReplace 锁定机制保证一致性
- ✅ 工具化快速配置：HUD Quick Config 工具一键生成/绑定/校验
- ✅ Excel 导出：CdbImportRoot 配置 + 路径镜像 + 双层防护校验

**DoD（Definition of Done）**：
1. ✅ Import All 生成 ItemCatalog.asset，无 schemaVersion/provider 错误
2. ✅ 场景放置 5 个 Ability Pickup：前 4 个直接入槽，第 5 个弹替换面板
3. ✅ HUD 实时更新：能力槽图标、血瓶计数、血条
4. ✅ EditMode + PlayMode 测试全绿
5. ✅ 导入/运行日志无异常，导入产物无手改
6. ✅ Excel 导出正常工作，CdbImportRoot 校验失败时正确中止


#### 核心架构变更（0.5）

**Status 系统架构**:
```
Status.cdb（状态定义表）
    │
    ├── id: 状态唯一标识
    ├── stackRule: Refresh/Add/Ignore/Replace
    └── modifiersJson: 属性修改器（JSON 对象）
        │
        ▼
StatusDataProvider.Import()
        │
        ▼
StatusCatalog.asset（StatusDefinition[]）
        │
        ▼
CastleDbService.TryGetStatus(statusId, out StatusDefinition)
        │
        ▼
Runtime（StatModifierLayer/ActiveBuffAbility）
```

**Relic 系统架构**:
```
Relic.cdb（遗物定义表）
    │
    ├── id: 遗物唯一标识
    ├── kind: Shield/StatBoost/...
    └── paramsJson: 遗物参数（JSON 对象）
        │
        ▼
RelicDataProvider.Import()
        │
        ▼
RelicCatalog.asset（RelicDefinition[]）
        │
        ▼
CastleDbService.TryGetRelic(relicId, out RelicDefinition)
        │
        ▼
Runtime（RelicController/被动效果）
```

**能力扩展架构**:
```
PlayerAbility.cdb
    │
    ├── kind: Attack/Dash/Buff/Summon/AttackOverride
    ├── paramsJson: 能力参数（按 kind 不同）
    └── events: onCast/onHit/onKill/onDamaged
        │
        ▼
AbilityDataProvider.Import()
        │
        ▼
AbilityCatalog.asset（AbilityDefinition[]）
        │
        ▼
AbilityRegistry.CreateAbility()
        │
        ├─ kind=Dash → DashAbility
        ├─ kind=Buff → ActiveBuffAbility
        ├─ kind=Summon → SummonAbility
        └─ kind=AttackOverride → AttackOverrideAbility
```

**输入设备切换架构**:
```
InputDeviceSwitcher（自动检测最后使用设备）
    │
    ├─ 检测到键盘/鼠标 → 切换到 Gameplay ActionMap
    └─ 检测到手柄 → 切换到 Gamepad ActionMap
        │
        ▼
HudPresenter.OnDeviceChanged()
        │
        ▼
InputIconCatalog.GetIcon(actionName, deviceType)
        │
        ▼
HUD 更新键位图标（Slot_0~3 显示对应设备图标）
```

#### 核心架构变更（0.4）

**Item 系统架构**:
```
Item.cdb（Item 定义表）
    │
    ├── itemType: Ability/Consumable/Material
    ├── abilityId（Ability 类型必填，引用 PlayerAbility.id）
    └── consumeEffectJson（Consumable 效果，JSON 对象）
        │
        ▼
ItemDataProvider.Import()
        │
        ▼
ItemCatalog.asset（ItemDefinition[]）
        │
        ▼
CastleDbService.TryGetItem(itemId, out ItemDefinition)
        │
        ▼
Runtime（Inventory/Pickup/HUD/EquipmentController）
```

**拾取与替换流程**:
```
ItemPickup（场景拾取物）
    │
    ├─ OnTriggerEnter2D(Player)
    │   └─ PlayerInventory.TryPickup(PickupRequest)
    │       │
    │       ├─ Success → 销毁 Pickup
    │       ├─ RequireReplace（槽满）→ 锁定 Pickup
    │       │   └─ ReplaceController.BeginReplace(PendingReplaceContext)
    │       │       ├─ 切换输入到 UI ActionMap
    │       │       ├─ 显示替换面板（当前 4 槽 + 待入槽物品）
    │       │       ├─ 等待用户选择槽位（1~4 键）
    │       │       ├─ Confirm → EquipAbilityItemToSlot → 销毁 Pickup
    │       │       └─ Cancel（ESC）→ 解锁 Pickup
    │       │
    │       └─ Failed_XXX → 不销毁/不锁定
```

**HUD 数据驱动架构**:
```
HudBinding.asset（资源入口，指向 HUDCanvas.prefab）
    │
    ▼
HUDCanvas Prefab（包含 HudRefs 组件）
    ├── BottomLeft/AbilityBar/Slot_0..3（Image）
    ├── BottomLeft/PotionWidget/CountText（TMP_Text）
    ├── BottomCenter/HealthBar/Fill（Image fillAmount）
    ├── BottomCenter/EnergyBar（默认隐藏）
    └── Overlay/AbilityReplacePanel（默认隐藏）
        │
        ▼
HudPresenter.Initialize(items, refs, inv, dmg)
    ├── 订阅 PlayerInventory.OnAbilitySlotChanged
    ├── 订阅 PlayerInventory.OnPotionCountChanged
    ├── 订阅 Damageable.OnHealthChanged
    └── RefreshAll()（初始刷新）
```

**GameBootstrap 装配顺序（硬契约）**:
```
GameBootstrap.Awake()
    1) 加载 ItemCatalog.asset → 初始化 CastleDbService
    2) player.Inventory.Initialize(items, cfg)
    3) 加载 HudBinding.asset → 实例化/定位 HUD
    4) hudPresenter.Initialize(items, hudRefs, inv, dmg)
    5) replaceController.Initialize(items, hudRefs, player, hudPresenter)
    6) equipmentController.Initialize(items, abilitySystem, inv)
        └── Start() → SyncAllSlotsToAbilities()
```

**核心组件（0.5 新增）**:
| 组件 | 路径 | 职责 |
|------|------|------|
| `StatusDefinition` | `Runtime/Dtos/StatusDefinition.cs` | 状态数据结构（id/displayName/duration/stackRule/modifiersJson） |
| `StatusCatalog` | `Runtime/StatusCatalog.cs` | 状态目录 ScriptableObject（TryGetStatus/GetAllStatuses） |
| `StatusDataProvider` | `Editor/Providers/StatusDataProvider.cs` | 处理 Status.cdb 导入 |
| `RelicDefinition` | `Runtime/Dtos/RelicDefinition.cs` | 遗物数据结构（id/kind/paramsJson） |
| `RelicCatalog` | `Runtime/RelicCatalog.cs` | 遗物目录 ScriptableObject（TryGetRelic/GetAllRelics） |
| `RelicDataProvider` | `Editor/Providers/RelicDataProvider.cs` | 处理 Relic.cdb 导入 |
| `StatModifierLayer` | `Scripts/Player/StatModifierLayer.cs` | 属性修改器系统（叠加/刷新/替换规则） |
| `DashAbility` | `Scripts/Player/Abilities/DashAbility.cs` | 冲刺能力实现 |
| `ActiveBuffAbility` | `Scripts/Player/Abilities/ActiveBuffAbility.cs` | 主动增益能力实现 |
| `SummonAbility` | `Scripts/Player/Abilities/SummonAbility.cs` | 召唤能力实现 |
| `AttackOverrideAbility` | `Scripts/Player/Abilities/AttackOverrideAbility.cs` | 攻击方式替换能力实现 |
| `InputDeviceSwitcher` | `Scripts/Input/InputDeviceSwitcher.cs` | 输入设备自动切换器 |
| `InputIconCatalog` | `Scripts/Data/InputIconCatalog.cs` | 输入图标配置（键盘/手柄） |

**核心组件（0.4）**:
| 组件 | 路径 | 职责 |
|------|------|------|
| `ItemDefinition` | `Runtime/Dtos/ItemDefinition.cs` | Item 数据结构（id/displayName/itemType/abilityId/icon...） |
| `ItemCatalog` | `Runtime/ItemCatalog.cs` | Item 目录 ScriptableObject（TryGetItem/GetAllItems） |
| `ItemDataProvider` | `Editor/Providers/ItemDataProvider.cs` | 处理 Item.cdb 导入 |
| `PlayerInventory` | `Scripts/Player/PlayerInventory.cs` | 玩家背包（4 槽 Ability + 血瓶计数） |
| `ItemPickup` | `Scripts/Gameplay/ItemPickup.cs` | 场景拾取物（Idle/Locked/Destroyed 状态机） |
| `PlayerEquipmentController` | `Scripts/Player/PlayerEquipmentController.cs` | 槽位 → AbilitySystem 映射 |
| `ReplaceController` | `Scripts/UI/ReplaceController.cs` | 替换流程状态机（Idle/Selecting） |
| `HudPresenter` | `Scripts/UI/HudPresenter.cs` | HUD 数据驱动呈现层 |
| `HudRefs` | `Scripts/UI/HudRefs.cs` | HUD 节点引用组件（挂在 HUDCanvas 根） |
| `HudBindingAsset` | `Scripts/Data/HudBindingAsset.cs` | HUD 资源入口 ScriptableObject |
| `PlayerContext` | `Scripts/Player/PlayerContext.cs` | 玩家服务统一暴露点（Inventory/Damageable/ReplaceController...） |
| `GameBootstrap` | `Scripts/Gameplay/GameBootstrap.cs` | Runtime 启动装配器（依赖注入唯一责任点） |

---

